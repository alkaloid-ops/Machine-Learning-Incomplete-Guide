
# 数据预处理

## 划分数据集

- 自动划分数据集
    
    ```python
    from sklearn.model_selection import train_test_split as TTS
    Xtrain, Xtest, ytrain, ytest = TTS(X, y, test_size=0.3, random_state=42) #常用0.3作为测试集数据比例
    
    #分层采样，使划分后的训练集和测试集有着近乎相同的数据分布(适用于小数据集)
    bins = pd.qcut(dataset['目标变量'], q=5, labels=False)
    train, test = train_test_split(housing, test_size=0.3, shuffle=True, stratify=bins, random_state=42)
    ```
    
- 手工划分数据集
- 当数据集存在重复主键时(如有重复的订单ID、用户ID等)需要手工划分数据集(即保证训练集的主键在测试集中不出现)(防止数据泄漏)
    
    ```python
    import random
    random.seed(42) #指定随机数种子
    train_id = data_set['主键特征名'].unique().tolist()
    test_id = random.sample(train_id, int(len(train_id)*0.3)) #从数据集中去重复后的主键中取30%作为测试集主键
    for i in test_id:
        train_id.remove(i) #训练集移除测试集的主键
    set(train_id) & set(test_id) #检查并确保训练集和测试集的主键不重叠
    
    data_set['分割依据'] = 'train' #添加分割依据辅助列
    for i in test_id:
        data_set.loc[data_set['主键特征名']==i, '分割依据'] = 'test'
    data_set['分割依据'].value_counts()
    train = data_set[data_set['分割依据'] == 'train']
    test = data_set[data_set['分割依据'] == 'test']
    ```
    

## 缺失值处理

### SimpleImputer

- 导入缺失值处理语句
    
    ```python
    from sklearn.impute import SimpleImputer
    ```
    
- SimpleImputer参数详解
    
    ```python
    SimpleImputer(
        missing_values=np.nan,     # 缺失值标识(数值类nan, 字符串类'missing'或None)
        strategy="mean",           # 填充策略(可选'median'中位数, 'most_frequent'众数, 'constant'自定义数值—需配合fill_value参数)
        fill_value=None,           # 自定义填充值
        copy=True,                 # 是否创建副本
        add_indicator=True,        # 是否添加二进制列指示缺失位置(保留缺失模式)(会生成新的特征列来记录标记, 值为二分类编码)
        keep_empty_features=False  # 是否保留全缺失的特征列(默认False为删除)
    )
    ```
    
- 拟合转换数据语句
    
    ```python
    X = SimpleImputer().fit_transform(X)
    ```
    

### KNNImputer

- 当特征间存在局部相关性, 以K近邻算法(用最相似的K个样本的均值/中位数)填充缺失值(任一特征中缺失值占比<20%)
- 使用前需标准化数据(主要针对连续变量)
- 导入KNNImputer语句
    
    ```python
    from sklearn.impute import KNNImputer
    ```
    
- KNNImputer参数详解
    
    ```python
    KNNImputer(
        missing_values=np.nan,  # 缺失值标识(数值类nan, 字符串类'missing'或None)
        n_neighbors='k近邻数量', #经验法则: 从5开始, 通过交叉验证调整
        weights='uniform', #uniform权重相等(文本/分类特征), 备选参数distance权重与距离成反比(距离越近权重越大)(金融/医疗数据)
        metric='nan_euclidean', #距离的计算方式, 备选参数manhattan, cosine(高维稀疏数据推荐使用)
        copy=True, #是否创建数据副本
        add_indicator=False, #是否添加缺失值指示器(标记缺失值)
        keep_empty_features=False #是否删除全缺失的特征列
    )
    ```
    
- 拟合转换数据语句
    
    ```python
    X = KNNImputer().fit_transform(X)
    ```
    

### IterativeImputer

- 根据模型回归预测算法来填充缺失值
- 导入IterativeImputer语句
    
    ```python
    from sklearn.experimental import enable_iterative_imputer
    from sklearn.impute import IterativeImputer
    ```
    
- IterativeImputer参数详解
    
    ```python
    IterativeImputer(
        estimator='指定算法模型', #默认BayesianRidge贝叶斯岭回归, LinearRegression线性回归, RandomForestRegressor随机森林回归, KNeighborsRegressorK近邻回归, XGBRegressorXGBoost回归
        missing_values=np.nan,  # 缺失值标识(数值类nan, 字符串类'missing'或None)
        sample_posterior=False, #False直接使用模型预测值, True从预测分布中随机采样(引入不确定性)
        max_iter=10, #最大迭代次数
        tol=0.001, #损失函数收敛阈值
        n_nearest_features=None, #限制建模时使用的特征数, None使用全部特征
        initial_strategy='mean', #初始化缺失值的方法, 备选参数median中位数, most_frequent众数, constant自定义(配合fill_value参数)
        fill_value=None, #自定义填充值
        imputation_order='ascending', #决定特征填充顺序, 默认按缺失率从低到高填充, 还可以random随机
        skip_complete=False, #是否跳过无缺失的特征列, 大数据集可跳过以加速计算
        min_value=-inf, #约束填充值的合理范围(如概率范围0-1, 年龄范围0-100), 默认负无穷到正无穷
        max_value=inf, #约束填充值的合理范围(如概率范围0-1, 年龄范围0-100), 默认负无穷到正无穷
        verbose=0, #过程输出
        random_state=42,
        add_indicator=False, #是否添加缺失值指示器(标记缺失值)
        keep_empty_features=False #是否删除全缺失的特征列
    )
    ```
    
- 拟合转换数据语句
    
    ```python
    X = IterativeImputer().fit_transform(X)
    ```
    

## 异常值检测

- 首先使用可视化来查看数据分布(单维特征逐一查看或高维特征降维查看), 验证其正态性, 分析异常值
- 使用三西格玛法, IQR法, DBSCAN聚类(基于密度的聚类算法来自动识别离群点), 隔离森林(专门检测异常值的集成学习算法—适合高维数据)

### **三西格玛法**

- 检测语句
    
    ```python
    #异常大值检测
    data[data > np.mean(data) + 3* np.std(data)]
    #异常小值检测
    data[data < np.mean(data) - 3* np.std(data)]
    ```
    

### IQR法

- 检测语句
    
    ```python
    Q1 = np.quantile(data, 0.25)
    Q3 = np.quantile(data, 0.75)
    IQR = Q3-Q1
    data[data < Q1-1.5*IQR] #异常小值
    data[data > Q3+1.5*IQR] #异常大值
    ```
    

## 样本不均衡

- 分类问题中预测标签的样本类别占比不均衡问题(常见于异常用户识别—异常用户占比为少数或极少数, 多数都为正常用户)
- 分配类别权重: 调整部分模型参数class_weight类别权重, 使模型更注重于少数类, 调整预测概率阈值, 并尽量使Recall召回率最大化和控制Precision精确率(如果模型没有class_weight参数需要另外方法处理样本不均衡, 如SMOTE)
- 可集成两个模型, 一个高召回率(初筛), 另一个高精确率(复核)
- 重采样SMOTE(over_sampling)(仅适用于分类标签是数值编码)
- 导入SMOTE语句
    
    ```python
    from imblearn.over_sampling import SMOTE
    ```
    
- SMOTE参数详解
    
    ```python
    SMOTE(
        sampling_strategy='auto', #采样策略: 备选参数'minority'使所有少数类达到多数类数量, 'not minority'除多数类外对所有类过采样, 或浮点数指定少数类样本数达到多数类的比例
        random_state=42,
        k_neighbors=5, #最近邻数量(值越小, 生成样本更接近原始样本, 多样性降低; 值越大, 生成样本更分散, 可能引入噪声)
        n_jobs=-1,
        k_neighbors_algorithm='auto',
        m_neighbors=10,
        out_step=0.5,
        kind='regular',
        svm_estimator=None,
        cluster_balance_threshold='auto'
    )
    ```
    
- 导入SMOTE语句
    
    ```python
    X, y = SMOTE().fit_resample(X_train, y_train)
    ```
    
