
# 特征工程

## 无量纲化

### 数据归一化

- 将数据收敛到0至1区间内(统一量纲), 但是对异常值敏感
- 导入数据归一化语句
    
    ```python
    from sklearn.preprocessing import MinMaxScaler
    ```
    
- 数据归一化参数详解
    
    ```python
    MinMaxScaler(
        feature_range=(0,1), #归一化数值范围, 默认0-1
        copy=True, #创建副本, 默认创建副本, False为直接原地修改
        clip=False #是否强制限制超出范围的数值, 如小于0的数值被强制为0, 大于1的数值被强制为1
    )
    #⚠️注意: 当数据特征矩阵为稀疏矩阵时, 不可执行归一化(可能会有最大值与最小值都为0, 以至于分母为0)
    ```
    
- 实例化并拟合转换数据语句
- ⚠️缺失值不参与计算, 转换后仍是缺失值
    
    ```python
    MMS = MinMaxScaler()
    MMS.fit(Xtrain) #当数据集特征较多时, 可能报错, 可尝试使用partial_fit
    Xtrain_New = MMS.transform(Xtrain)
    Xtest_New = MMS.transform(Xtest)
    ```
    
- 归一化后还原原始数据语句
    
    ```python
    MMS.inverse_transform(Xtrain_New) #如果MinMaxScaler参数开启了clip=True, 将无法还原那些被强制限制的数据
    ```
    
- 归一化公式
    
    $$
    x= \frac{x_i - min(x)}{max(x)-min(x)}
    $$
    
- 归一化还原公式
    
$$
x_i=x_{\mathrm{norm}}(\max(x)-\min(x))+\min(x)
$$
    

### 数据标准化

- 将数据转换为服从均值0, 标准差1的正态分布(大多数算法模型推荐使用标准化)
- 导入数据标准化语句
    
    ```python
    from sklearn.preprocessing import StandardScaler
    ```
    
- 数据标准化参数详解
    
    ```python
    StandardScaler(
        with_mean=True, #中心化
        with_std=True #缩放
    )
    #⚠️注意: 当数据特征矩阵为稀疏矩阵时, 不可执行中心化(with_mean=False), 仅执行缩放
    ```
    
- 实例化并拟合转换数据语句
- ⚠️缺失值不参与计算, 转换后仍是缺失值
    
    ```python
    SS = StandardScaler()
    SS.fit(Xtrain) #当数据集特征较多时, 可能报错, 可尝试使用partial_fit
    Xtrain_New = SS.transform(Xtrain)
    Xtest_New = SS.transform(Xtest)
    ```
    
- 标准化后还原原始数据语句
    
    ```python
    SS.inverse_transform(Xtrain_New) 
    ```
    
- 标准化公式
    
$$
x_i = \frac{x_i - \mu}{\sigma}
$$
    

### **鲁棒标准化**

- 对于特征矩阵中有较多的异常值时, 推荐使用鲁棒标准化
- 导入鲁棒标准化语句
    
    ```python
    from sklearn.preprocessing import RobustScaler
    ```
    
- 鲁棒标准化参数详解
    
    ```python
    RobustScaler(
        with_centering=True, #是否进行中心化(减去中位数)
        with_scaling=True, #是否进行缩放(除以四分位距IQR)
        quantile_range=(0.25,0.75), #计算IQR的分位数范围
        unit_variance=False, #是否缩放至单位方差(如果原数据接近正态分布, 此参数可使数据更服从正态分布, 但原数据不是正态分布, 此参数无论True还是False都不会是正态分布)
        copy=True #是否创建副本
    )
    ```
    
- 实例化并拟合转换数据
- ⚠️缺失值不参与计算, 转换后仍是缺失值
    
    ```python
    RS = RobustScaler()
    Xtrain = RS.fit_transform(Xtrain)
    ```
    
- 鲁棒标准化公式
    
    $$
    x_i = \frac{x_i - median}{IQR}
    $$
    

## 离散变量编码

### 标签编码

- 将分类预测标签转换为数值型
- 导入标签编码语句
    
    ```python
    from sklearn.preprocessing import LabelEncoder
    ```
    
- 实例化并拟合转换数据
    
    ```python
    LE = LabelEncoder()
    ytrain = LE.fit_transform(ytrain)
    ```
    
- 查看具体标签类别
    
    ```python
    LE.classes_
    ```
    
- 恢复原数据标签
    
    ```python
    LE.inverse_transform(ytrain)
    ```
    

### 序列编码

- 当特征标签为相互不独立时(即有大小高低等的关系), 将标签转换为有序数值型
- 导入序列编码语句
    
    ```python
    from sklearn.preprocessing import OrdinalEncoder
    ```
    
- 实例化并拟合转换数据
    
    ```python
    OE = OrdinalEncoder()
    Xtrain = OE.fit_transform(Xtrain)
    ```
    
- 查看具体标签类别
    
    ```python
    OE.categories_
    ```
    
- 恢复原数据标签
    
    ```python
    OE.inverse_transform(Xtrain)
    ```
    

### 独热编码

- 当特征标签为相互独立时(是其中一类时, 就不能是另外的类别), 将标签转换为新特征, 同时新特征的标签填充为二分类数值(是为1, 否为0)
- 导入独热编码语句
    
    ```python
    from sklearn.preprocessing import OneHotEncoder
    ```
    
- 实例化并拟合转换数据
    
    ```python
    OHE = OneHotEncoder(drop='first', #转换时，丢掉第一个变量以避免多重共线性（树类集成模型可不删除）
    										sparse_output=False, #sparse为True时返回稀疏矩阵
    										handle_unknown='ignore') #当训练集和测试集类别数不一致时，忽略
    OHE.fit(Xtrain)
    Xtrain = OHE.transform(Xtrain)
    Xtest = OHE.transform(Xtest)
    ```
    
- 查看具体标签类别
    
    ```python
    OHE.categories_[0] #调回特征名
    ```
    
- 恢复原数据标签
    
    ```python
    OHE.inverse_transform(Xtrain)
    ```
    

## 连续变量编码

### 二值化编码

- 设定某个阈值, 当连续变量大于等于阈值时为1, 小于阈值为0
- 导入二值化编码语句
    
    ```python
    from sklearn.preprocessing import Binarizer
    ```
    
- 二值化编码参数详解
    
    ```python
    Binarizer(
        threshold='阈值',
        copy=True #是否创建副本
    )
    ```
    
- 实例化并拟合转换数据
    
    ```python
    Bi = Binarizer()
    Xtrain = Bi.fit_transform(Xtrain)
    ```
    

### 分箱编码

- 将连续变量分成n个箱, 每个箱子是连续变量的一段区间(即给连续变量分组)
- 导入分箱编码语句
    
    ```python
    from sklearn.preprocessing import KBinsDiscretizer
    ```
    
- 分箱编码参数详解
    
    ```python
    KBinsDiscretizer(
        n_bins=5, #分箱数(为避免极端值, 分箱数小于等于样本数的平方根)
        strategy='quantile', #分箱策略
        encode='onehot', #编码方式, 默认'onehot'稀疏独热编码, 备选参数'onehot-dense'稠密独热编码, 'ordinal'序数编码
        dtype=np.float32, #输出数值类型
        subsample=None, #指定分箱采样数量, None为全部样本, 或指定任意整数样本数量
        random_state=42 #随机数种子只对分箱策略为'kmeans'有效
    )
    #分箱策略: 默认'quantile'等频分箱(分布偏斜时—有异常值—每个箱样本数一致), 备选参数'uniform'等宽分箱(分布均匀时—每个箱极差一致), 'kmeans'聚类分箱(数据聚类—使箱内数据到簇心距离相等)
    ```
    
- 实例化并拟合转换数据
    
    ```python
    KBD = KBinsDiscretizer()
    Xtrain = KBD.fit_transform(Xtrain)
    ```
    

### 分箱编码(金融异常风控二分类专用)

- 先以20分箱, 计算各箱正负样本数, 使用卡方检验对箱子(相邻两箱之间)检验
- 卡方检验语句
    
    ```python
    from scipy import stats
    stats.chi2_contingency(['第一个箱子正负样本数', '第二个箱子正负样本数']) #如[[10,20],[10,20]]
    #返回值中第一个为卡方值, 第二个为P值
    ```
    
- 找到最大的P值对应的那两箱进行合并, 合并后计算IV值, 再用卡方检验取最大P值对应的两个分箱合并(当P值<0.05不继续合并), 再计算IV值, 查看IV值的变化, 找到最佳IV值的箱子最少个数, 确定最佳分箱数n
- 将所有连续型变量以最佳分箱数n进行分箱, 将各个特征分箱的第一个箱子的下限替换为负无穷(-np.inf)和最后一个箱子的上限替换为正无穷(np.inf)以满足所有数值覆盖
- 最终计算各个特征各个分箱的WOE值, 将WOE值映射到原特征矩阵
- IV值计算
    - WOE证据权重(WOE大于0时正样本占比更高, 小于0时负样本占比更高)(分子为箱内正样本数占全部正样本数的比例, 分母为箱内负样本数占全部负样本数的比例)
        
        $$
        WOE_i = In(\frac{P_i/TotalP}{N_i/TotalN})
        $$
        
    - 单个箱的IV分量(Information Value)(单箱的预测价值或贡献度)
        
        $$
        IV_i = (P_i/TotalP - N_i/TotalN) \times WOE_i
        $$
        
    - 总IV值
        
        $$
        IV = \sum^n_{i=1} IV_i
        $$
        
    - 总IV低于0.02为无预测能力(可删除); 总IV介于0.02至0.1之间为弱预测能力(可保留谨慎使用); 总IV介于0.1至0.3之间为中预测能力(主要特征); 总IV高于0.3为高预测能力
- 总流程代码
    
    ```python
    import pandas as pd
    import numpy as np
    from scipy.stats import chi2_contingency
    
    def auto_binning(df, col, target, max_bins=5, min_bins=3):
        """自动分箱并返回WOE映射字典"""
        # 初始等频分箱(10箱)
        df['temp_bin'], bins = pd.qcut(df[col], q=10, duplicates='drop', retbins=True)
        
        # 递归合并分箱
        while True:
            # 计算每箱的坏样本率
            bin_stats = df.groupby('temp_bin')[target].agg(['count', 'mean'])
            bin_stats = bin_stats.rename(columns={'mean': 'bad_rate'})
            
            # 计算相邻箱的卡方p值
            p_values = []
            bins_list = sorted(bin_stats.index.categories)
            for i in range(len(bins_list)-1):
                cont_table = pd.crosstab(
                    df['temp_bin'].isin([bins_list[i], bins_list[i+1]]),
                    df[target]
                )
                _, p, _, _ = chi2_contingency(cont_table)
                p_values.append(p)
            
            # 停止条件: 无显著相似箱 or 达到最小分箱数
            if max(p_values) < 0.05 or len(bins_list) <= min_bins:
                break
                
            # 合并p值最大的相邻箱
            merge_idx = np.argmax(p_values)
            merge_bins = [bins_list[merge_idx], bins_list[merge_idx+1]]
            df['temp_bin'] = df['temp_bin'].cat.add_categories(['merged_bin'])
            df.loc[df['temp_bin'].isin(merge_bins), 'temp_bin'] = 'merged_bin'
            df['temp_bin'] = df['temp_bin'].cat.remove_unused_categories()
        
        # 计算最终WOE
        total_bad = df[target].sum()
        total_good = len(df) - total_bad
        woe_map = {}
        for bin_name, group in df.groupby('temp_bin'):
            bad_cnt = group[target].sum()
            good_cnt = len(group) - bad_cnt
            woe = np.log((good_cnt / total_good) / (bad_cnt / total_bad))
            woe_map[bin_name] = woe
        
        return woe_map, bin_stats
    
    # 使用示例
    woe_mapping, bin_stats = auto_binning(
        df=loan_data, 
        col='income', 
        target='default', 
        max_bins=5,
        min_bins=3
    )
    
    # 将原始特征转换为WOE
    loan_data['income_woe'] = loan_data['income'].apply(
        lambda x: next(woe for bin_range, woe in woe_mapping.items() if x in bin_range)
    )
    ```
    

## 文本数据编码

### 单词计数向量(词向量)

- 提取文本中的词(去重复)作为各个特征, 统计计算各个词特征的出现次数, 组成特征矩阵
- 导入单词计数向量语句
    
    ```python
    from sklearn.feature_extraction.text import CountVectorizer
    ```
    
- 单词计数向量参数详解
    
    ```python
    CountVectorizer(
        input='content', #直接处理文本内容, 备选参数filename文件路径
        encoding='utf-8', #文本编码
        decode_error='strict', #解码错误抛出错误
        strip_accents=None, #移除重音符号
        lowercase=True, #转小写字母
        preprocessor=None, #自定义预处理函数
        tokenizer=None, #自定义分词
        token_pattern='r"(?u)\b\w\w+\b"', #定义词的正则表达式
        stop_words=None, #停用词列表
        ngram_range=(1, ...) #N元语法
        analyzer='word', #分析单元, word词级别, char字符级别
        max_df=1, #忽略文档频率超过该值的词, 0.95表示忽略出现在>95%文档中的词(常见词)
        min_df=1, #忽略文档频率低于该值的词, 0.01表示忽略出现在<1%文档中的词(罕见词)
        max_features=None, #保留词频最高的前N个特征, 默认None保留所有词
        vocabulary=None, #手动指定词汇表
        binary=False, #是否生成二值向量(1表示词存在0表示不存在), False输出词频计数
        dtype=<class 'numpy.int64'>, #输出计数数值类型
    )
    #标点符号不参与处理, 全部忽略
    ```
    
- 拟合转换文本数据
    
    ```python
    CV = CountVectorizer().fit_transform(Xtrain)
    ```
    
- 获取词特征名称(去重复后的)
    
    ```python
    CountVectorizer().get_feature_names_
    ```
    

### 词频逆文档频率TF-IDF

- 提取文本中的词(去重复)作为各个特征, 统计各个词文本中所占比例(反比), 以压制高频无意义的词的出现过高占比(如英语中的语法虚词, 汉语中的’的’)
- TF：一个词在文本出现的次数占比（TF=单词出现次数/文档总单词数量）
- IDF：一个词在文本中出现的次数越少，则越重要（IDF=log(文本数量/单词出现的文本数量)）
- 导入TF-IDF语句
    
    ```python
    from sklearn.feature_extraction.text import TfidfVectorizer
    ```
    
- TF-IDF参数详解
    
    ```python
    TfidfVectorizer(
        input='content', #直接处理文本内容, 备选参数filename文件路径
        encoding='utf-8', #文本编码
        decode_error='strict', #解码错误抛出错误
        strip_accents=None, #移除重音符号
        lowercase=True, #转小写字母
        preprocessor=None, #自定义预处理函数
        tokenizer=None, #自定义分词
        token_pattern='r"(?u)\b\w\w+\b"', #定义词的正则表达式
        analyzer='word', #分析单元, word词级别, char字符级别
        stop_words=None, #停用词列表
        ngram_range=(1, ...) #N元语法
        max_df=1, #忽略文档频率超过该值的词, 0.95表示忽略出现在>95%文档中的词(常见词)
        min_df=1, #忽略文档频率低于该值的词, 0.01表示忽略出现在<1%文档中的词(罕见词)
        max_features=None, #保留词频最高的前N个特征, 默认None保留所有词
        vocabulary=None, #手动指定词汇表
        binary=False, #是否生成二值向量(1表示词存在0表示不存在), False输出词频计数
        dtype='numpy.float64', #输出计数数值类型
        norm='l2', #TF-IDF向量的归一化方式, 备选参数l1或None不进行归一化
        use_idf=True, #是否启用IDF逆文档频率加权, False退化为CountVectorizer归一化版本
        smooth_idf=True, #IDF计算是否添加平滑项
        sublinear_tf=False #是否对TF词频进行亚线性变换(可抑制高频词的影响)
    )
    ```
    
- 拟合转换文本数据
    
    ```python
    TFIDF = TfidfVectorizer().fit_transform(Xtrain)
    ```
    
- 获取词特征名称(去重复后的)
    
    ```python
    TfidfVectorizer().get_feature_names_
    ```
    

## 特征衍生

### 时序特征衍生

- 自然周期划分, 业务周期划分
- 拆分年、月、季度、季节、星期、年当中的周数、是否节假日、是否周末等
- 更为详细的小时(更进一步划分为日间/夜间或凌晨/上午/下午/晚上(6小时为一个单位)的离散变量)、分钟(大于半小时为1, 小于等于半小时为0)
- 计算日期之间的时间差

### 多项式特征衍生

- 针对连续变量使用(需要先无量纲化)
- 一般重要的特征考虑二阶, 较为重要的特征考虑三阶(最高尽量不超过三阶)
- 导入特征多项式语句
    
    ```python
    from sklearn.preprocessing import PolynomialFeatures
    ```
    
- 多项式参数详解
    
    ```python
    PolynomialFeatures(
        degree=2, #多项式指数
        interaction_only=False, #仅交叉项(x1x2, x1^2,x2^2被排除), 开启后可一定程度避免多重共线性
        include_bias=True, #计算偏置b
        order='C' #排序, c为多项式在前, 一次项在后, f相反
    )
    ```
    
- 拟合转换数据
    
    ```python
    PF = PolynomialFeatures().fit_transform(Xtrain)
    ```
    
- 获取训练后模型对应特征名(用作查看对各特征的计算指数)
    
    ```python
    PF.get_feature_names(Xtrain.columns)
    ```
    

### 交叉组合特征衍生

- 将多个离散特征变量组合(如yes&yes&no), 并转换成独热编码
- 将两个或多个连续变量进行数学运算构建新特征
- 连续变量特征之间除法计算时, 为避免分母为0, 可以在分母加入很小的平滑指数

### 分组统计特征衍生

- 聚合分组计算groupby
- 聚合分组计算各组的分位数、最大值、最小值、均值、中位数、偏度、峰度等
- 聚合分组计算各组标准差/各组均值=变异系数

### NLP文本类特征衍生

- 考虑使用关联规则的非结构化事件数据处理
- 使用单词计数向量(词向量)或TF-IDF

## 特征选择

### 方差过滤法

- 计算方差过滤掉特征方差近乎为0的特征(降维处理)
- 方差过滤适合需要遍历所有特征的算法模型(如: KNN, 决策树, SVM, 线性回归, 逻辑回归, 神经网络), 目标降低计算时间成本
- 导入方差过滤语句
    
    ```python
    from sklearn.feature_selection import VarianceThreshold
    ```
    
- 方差过滤参数详解
    
    ```python
    VarianceThreshold(threshold='方差过滤阈值') #默认阈值为0, 通常取值为接近0的很小的数值
    #最优方差阈值需要多次尝试, 或for循环穷举(需要一定的计算时间成本)
    ```
    
- 实例化并拟合转换数据
    
    ```python
    VT = VarianceThreshold()
    Xtrain = VT.fit_transform(Xtrain)
    ```
    
- 伯努利分布离散变量(二分类)方差计算公式(其中p为某一类别占总量的比例)
    
    $$
    Var[X] = p(1-p)
    $$
    

### 卡方过滤法

- 卡方只能计算非负数值(推荐在使用方差过滤后作为进一步过滤使用来降维)
- 导入卡方过滤语句
    
    ```python
    from sklearn.feature_selection import chi2
    from sklearn.feature_selection import SelectKBest
    ```
    
- 计算卡方值与p值语句
    
    ```python
    chi_value, p_value = chi2(Xtrain, ytrain)
    ```
    
- 比较p值与显著性水平alpha (设alpha=0.05, 若p小于等于0.05, 拒绝零假设(即p值对应的那个特征与预测标签是相关的), 反之p大于0.05, 接受零假设(即p值对应的那个特征与预测标签是独立的))
- 计算k参数语句
    
    ```python
    k = (p_value<=0.05).sum()
    ```
    
- 拟合转换数据
    
    ```python
    Xtrain = SelectKBest(chi2, k='p值小于等于显著性水平的数量').fit_transform(Xtrain, ytrain)
    ```
    

### F检验/方差齐性检验/ANOVA

- 使用F检验前需要将数据集标准化使之服从正态分布
- 导入F检验语句
    
    ```python
    from sklearn.feature_selection import f_regression #回归(连续变量)
    from sklearn.feature_selection import f_classif #分类(离散变量)
    from sklearn.feature_selection import SelectKBest
    ```
    
- 计算f值与p值语句
    
    ```python
    f_value, p_value = f_regression(Xtrain, ytrain)
    f_value, p_value = f_classif(Xtrain, ytrain)
    ```
    
- 计算k参数语句
    
    ```python
    k = (p_value<=0.05).sum()
    ```
    
- 拟合转换数据
    
    ```python
    Xtrain = SelectKBest('f_regression或f_classif', k='p值小于等于显著性水平的数量').fit_transform(Xtrain, ytrain)
    ```
    

### 互信息法

- 导入互信息语句
    
    ```python
    from sklearn.feature_selection import mutual_info_regression as MIR #回归(连续变量)
    from sklearn.feature_selection import mutual_info_classif as MIC #分类(离散变量)
    from sklearn.feature_selection import SelectKBest
    ```
    
- 计算k参数语句
    
    ```python
    value = MIR(Xtrain, ytrain)
    value = MIC(Xtrain, ytrain)
    #返回值小于等于0为相互独立, 大于0为相互相关
    k = np.sum(value > 0)
    ```
    
- 拟合转换数据
    
    ```python
    Xtrain = SelectKBest('MIR或MIC', k='相互相关特征数量').fit_transform(Xtrain, ytrain)
    ```
    

### 顺序特征选择

- 通过逐步添加(前向选择)或移除(后向消除)特征来寻找最优特征子集(特征小于50时, 更多的特征推荐使用嵌入法或包装法)
- 前向选择: 从空集开始, 逐步添加能使模型提升的特征
- 反向选择: 从所有特征开始, 逐步移除不重要的特征
- 导入顺序特征选择语句
    
    ```python
    from mlxtend.feature_selection import SequentialFeatureSelector as SFS
    ```
    
- 顺序特征选择参数详解
    
    ```python
    SFS(
        estimator='模型名',
        k_features='best', #自动选择最优特征数量
        forward=True, #True前向选择, False反向选择
        floating=False, #是否启用浮动选择(允许回溯调整)(即移除的特征再添加回来进行调整)
        verbose=0
        scoring='评估指标'
        cv=5
        n_jobs=-1
        pre_dispatch='2*n_jobs', #控制并行任务分发
        clone_estimator=True, #是否在每次迭代克隆模型(避免原始模型被修改)
        fixed_features=None #固定特征(固定的特征不会被移除)
    )
    ```
    
- 拟合训练数据
    
    ```python
    SFS = SFS().fit(Xtrain, ytrain)
    ```
    
- 获取选择出的特征布尔
    
    ```python
    SFS.get_support()
    ```
    

### 嵌入法Enbeded

- 将所有特征输入模型, 查看模型训练后对于各个特征的重要性来做特征选择(每次都将所有特征输入)
- 导入嵌入法语句
    
    ```python
    from sklearn.feature_selection import SelectFromModel
    ```
    
- 嵌入法参数详解
    
    ```python
    SelectFromModel(
        estimator='模型名', #传入的模型需要已实例化, 并且模型需要具备feature_importances_和coef_属性, 如线性模型和树模型等
        threshold='特征重要性阈值', #低于这个阈值的特征都将被删除(属于超参数, 通过for循环遍历0到模型特征重要性的最大值之间的等差数列)
        prefit=False, #模型是否已预训练
        norm_order=1, #系数规范化阶数(1为L1范数, 2为L2范数)配合线性模型使用
        max_features=None, #最大特征选择数, 默认None为取全部特征
        importance_getter='auto' #重要性获取方式, 默认auto自动检测
    )
    ```
    
- 实例化并拟合转换数据
    
    ```python
    SFM = SelectFromModel().fit_transform(X, y)
    ```
    

### 包装法Wrapper

- 首先将所有特征输入评估器, 评估器会计算各个特征的重要性, 在下一次特征输入前会将不重要的特征排除掉, 递归运行
- 导入包装法语句
    
    ```python
    from sklearn.feature_selection import RFE
    from sklearn.feature_selection import RFECV #增加交叉验证
    ```
    
- 包装法参数详解
    
    ```python
    RFE(
        estimator='模型名', #传入的模型需要已实例化, 并且模型需要具备feature_importances_和coef_属性, 如线性模型和树模型等
        n_features_to_select='选择的特征个数'
        step='每次迭代希望去掉的特征个数'
        verbose=0
        importance_getter='auto'
    )
    ```
    
- 拟合数据
    
    ```python
    RFE = RFE().fit(X, y)
    ```
    
- 转换数据
    
    ```python
    X = RFE.transform(X)
    ```
    
- 返回所有特征是否被选中的布尔值矩阵
    
    ```python
    RFE.support_
    ```
    
- 返回评估器多次运行后, 特征的综合重要性排名
    
    ```python
    RFE.ranking_
    ```
    

### 特征重要性

- 通过随机打乱特征值并观察模型性能变化来度量特征重要性(在嵌入法或包装法筛选特征后进一步评估特征的重要性)
- 导入特征重要性语句
    
    ```python
    from sklearn.inspection import permutation_importance
    ```
    
- 特征重要性参数详解
    
    ```python
    permutation_importance(
        estimator='训练好的模型',
        X='验证集或测试集',
        y='目标变量',
        scoring='评估指标',
        n_repeats=5, #每个特征随机打乱的次数(数值越大精度越高但计算时间越长)
        n_jobs=-1,
        random_state=42,
        sample_weight='样本不均衡权重配置',
        max_samples=1 #用于计算的样本比例(大数据集使用0.5-0.8)
    )
    ```
    
- 函数返回特征重要性均值(值越大越重要), 重要性标准差, 所有重复的原始结果三个属性

## 多重共线性

- 特征矩阵的行向量之间存在在相关性, 使得矩阵初等变换后导致矩阵对角线元素出现0或接近0的小数, 进一步导致矩阵的行列式为0(矩阵没有逆)或无限接近0, 这样会影响模型拟合数据导致预测失真
- 检查特征矩阵多重共线性(使用方差膨胀因子计算)
- VIF判断标准: 小于5的特征基本没有共线性, 大于5的特征严重共线性, 5-10之间为中等程度的共线性
- 解决方案: 将其删除或组合计算成新的特征, 或使用PCA降维, 维度参数为浮点数0.95(仅保留95%的方差)
- 导入方差膨胀因子语句
    
    ```python
    from statsmodels.stats.outliers_influence import variance_inflation_factor as vif
    ```
    
- 计算方差膨胀因子
    
    ```python
    X = np.array(Xtrain['从相关系数矩阵中筛选强相关的特征'])
    vif_list = [vif(X,i) for i in range(X.shape[1])]
    ```
    

## 组合列处理器-多特征编码同时处理

- 导入组合列处理器语句
    
    ```python
    from sklearn.compose import ColumnTransformer
    ```
    
- 组合列处理器参数详解
    
    ```python
    ColumnTransformer(
        transformers='转换器名称', #元组形式传入('cat', OneHotEncoder(), ['gender', 'city'])
        remainder='passthrough', #未处理的特征(直接保留原始)
        sparse_threshold=0, #输出是否为稀疏矩阵, 0为不输出稀疏矩阵, 稀疏矩阵需要输入具体密度(取值0-1)
        n_jobs=-1, #计算线程数
        transformer_weights=None, #对特定转换器的输出加权(加上特征重要性)
        verbose=0, #输出过程
        verbose_feature_names_out=True #控制输出特征命名格式(包含转换器名称前缀)
    )
    ```
    
- 拟合转换数据
    
    ```python
    X = ColumnTransformer().fit_transform(X)
    ```
    
