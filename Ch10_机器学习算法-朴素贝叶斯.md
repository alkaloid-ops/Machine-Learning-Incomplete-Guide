
# 朴素贝叶斯

- 通用贝叶斯公式

$$
P(y|x)=\frac{P(y)P(x|y)}{P(x)}
$$

- 朴素贝叶斯公式 (基于通用贝叶斯，将各特征独立 (朴素)，只计算各x特征取值与y类别的发生概率的乘积)（其中分母是特征取值的全部概率，预测类别时比较分子即可出结果，分母可忽略）

$$
P(y_i|x_i)=\frac{P(y_i)\prod^n_{i=1}P(x_i|y_i)}{P(x_i)}
\Rightarrow
P(y_i|x_I)=P(y_i)\prod^n_{i=1}P(x_i|y_i)（输出的是概率，未归一化）
$$

- 当某一特征取值为0时，导致朴素贝叶斯分子乘积为0，以至于预测概率为0，这时加入拉普拉斯平滑指数（取值1或0.1或0.01）防止概率为0

$$
P(y_i|x_i)=\frac{count(x_i|y_i)+\alpha}{count(x_i)+\alpha K}（其中K特征列数量，\alpha 为平滑指数）
$$

## 高斯朴素贝叶斯

- 导入高斯朴素贝叶斯语句
    
    ```python
    from sklearn.naive_bayes import GaussianNB
    ```
    
- 高斯朴素贝叶斯参数详解
    
    ```python
    GaussianNB(priors=None, var_smoothing=1e-09) #第一参数为先验概率(默认None自行根据数据计算先验概率P(A)), 第二参数默认即可
    ```
    
- 模型拟合训练数据
    
    ```python
    GNB = GaussianNB().fit(Xtrain,ytrain)
    ```
    
- 获取模型预测结果
    
    ```python
    GNB.predict(Xtest) #标签值
    GNB.predict_proba(Xtest) #概率值
    ```
    

## 多项式朴素贝叶斯

- 该模型不接受特征矩阵中有负值(需要归一化)
- 基于计数来预估概率(特征矩阵倾向于稀疏矩阵, 对各个特征的次数来计数)并预测标签
- 计算方式: 某一类下某一特征出现的次数+平滑指数/该类下所有特征出现的总次数(含重复)+平滑指数x该类下的特征数量
- 平滑指数说明: 为防止某一类某一特征出现0次导致分子为0以至于概率为0使模型无法进一步计算, 分子分母等价添加平滑指数(拉普拉斯平滑指数a=1)
- 导入多项式朴素贝叶斯语句
    
    ```python
    from sklearn.naive_bayes import MultinomialNB
    ```
    
- 多项式朴素贝叶斯参数详解
    
    ```python
    MultinomialNB(
        alpha=1, #平滑指数:(小数据集a大于等于1, 大数据集a取0.5-1, 文本类a=1默认)
        force_alpha=False, #强制保持用户设置的alpha值, 通常保持默认即可
        fit_prior=True, #是否从数据中学习类别的先验概率(数据分布不均衡必须Ture, 分布均衡可False)
        class_prior=None #自定义类别的先验概率(可输入基于现实经验主义的普遍概率-列表形式)
    )
    ```
    
- 获取各类别的对数先验概率(各类别对数先验概率接近时表示样本分布较均衡)
    
    ```python
    MNB.class_log_prior_ #返回值为负值
    ```
    
- 对数先验概率转换为正常概率
    
    ```python
    np.exp(MNB.class_log_prior_) #正常概率-正值
    ```
    
- 获取各类别下各个特征的对数概率(也是特征的权重)
    
    ```python
    MNB.feature_log_prob_ #返回值为负值的数组(二分类二特征为2x2, 三分类三特征为3x3, 以此类推)
    ```
    
- 获取各类别下的样本数
    
    ```python
    MNB.class_count_
    ```
    

## 伯努利朴素贝叶斯

- 该模型不接受特征矩阵中有负值(需要归一化)
- 导入伯努利朴素贝叶斯语句
    
    ```python
    from sklearn.naive_bayes import BernoulliNB
    ```
    
- 伯努利朴素贝叶斯参数详解
    
    ```python
    BernoulliNB(
        alpha=1, #平滑指数:(小数据集a大于等于1, 大数据集a取0.5-1, 文本类a=1默认)
        force_alpha=False, #强制保持用户设置的alpha值, 通常保持默认即可
        binarize=0, #二值化阈值(将连续变量大于阈值为1, 其他为0)
        fit_prior=True, #是否从数据中学习类别的先验概率(数据分布不均衡必须Ture, 分布均衡可False)
        class_prior=None #自定义类别的先验概率(可输入基于现实经验主义的普遍概率-列表形式)
    )
    ```
    

## 补集朴素贝叶斯

- 专用于处理分类样本不均衡问题
- 与多项式朴素贝叶斯相反的计算方式(对于二分类, 多项式计算类别0就是类别0, 而补集计算类别0就是计算类别1)
- 计算方式: 某一类的另一类下某一特征出现的次数+平滑指数/该类的另一类下所有特征出现的总次数(含重复)+平滑指数x该类下的特征数量
- 导入补集朴素贝叶斯语句
    
    ```python
    from sklearn.naive_bayes import ComplementNB
    ```
    
- 补集朴素贝叶斯参数详解
    
    ```python
    ComplementNB(
        alpha=1, #平滑指数:(小数据集a大于等于1, 大数据集a取0.5-1, 文本类a=1默认)
        force_alpha=False, #强制保持用户设置的alpha值, 通常保持默认即可
        fit_prior=True, #是否从数据中学习类别的先验概率(数据分布不均衡必须Ture, 分布均衡可False)
        class_prior=None #自定义类别的先验概率(可输入基于现实经验主义的普遍概率-列表形式)
        norm=False, #是否对特征权重进行L2归一化
    )
    #归一化权重计算方式: 各特征权重向量/各特征权重向量的L2范数(高维度文本数据/样本类别分布极不均衡/需要概率校准时开启归一化权重)
    ```
    
- 获取各类别下各个特征的对数概率(也是特征的权重)
    
    ```python
    MNB.feature_log_prob_ #返回值为负值的数组(二分类二特征为2x2, 三分类三特征为3x3, 以此类推)
    ```
    
