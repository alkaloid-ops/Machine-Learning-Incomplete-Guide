

# 决策树

- Gini系数公式
    
    $$
    Gini=1-\sum^n_{i=1}P_i^2
    $$
    
- Entropy信息熵公式*(*其中*log*前为真实概率*, log*里是预测概率*)*
    
    $$
    Entropy=-\sum^n_{i=1}P_ilog_2(P_i)
    $$
    
- 信息增益计算(其中w为节点权重或单一节点的样本数量占双节点样本总量的比例, H为计算各节点的熵)
    
    $$
    H(P_{root})-[w_{left}H(P_{left})+w_{right}H(P_{right})]
    $$
    

## 回归树

- 导入回归树模型语句:
    
    ```python
    from sklearn.tree import DecisionTreeRegressor as DTR
    ```
    
- 回归树模型参数详解
    
    ```python
    DTR = DTR(
        criterion='squared_error', #(默认均方误差), 可选'friedman_mse'改进的均方误差—用于梯度提升,'absolute_error'平均绝对误差,'poisson'泊松偏差—用于用于预测正整数或计数类数据,(大多数回归任务首选‘squared_error’)
        splitter='best', #(默认‘best’), (常规场景默认‘best’, 大型数据集/防止过拟合选‘random’)
        max_depth=3, #(默认None: 表示树无限分裂直到信息不纯度为0或使用完所有训练集的特征—容易过拟合), 常用值3-10, 需要交叉验证和网格搜索调优参数
        max_features=0.8, #(默认None: 表示最优分裂时考虑所有特征数量)(尤其是高维度特征数据集来限制特征数量), 可选浮点数表示使用多少百分比的特征
        min_samples_leaf=10, #叶节点所需的最小样本数(即分裂到最后叶子节点时上一个节点至少要有指定数量的样本数量)默认为1(适合小数据集), 可调5-20(适合大数据集并防止过拟合)
        min_samples_split=10, #节点可分裂所需的最小样本数(即从上一个节点中至少有指定数量的样本数量才可以分裂), 默认为2(节点至少需2个样本才尝试分裂), 可调10-50(适合大数据集)
        min_impurity_decrease=0, #每次分裂要求必须降低的不纯度阈值, 一般默认即可
        random_state=42, #(默认None: 表示每次结果都随机), 指定随机种子数42, 以固定随机性
        
        #模型样本权重参数
        min_weight_fraction_leaf=0, #叶节点样本权重和的最小比例, 除非样本权重不均, 否则不调
    
        #以上是重要参数, 以下是可选调参数
        
        max_leaf_nodes=None, #(默认None: 表示无限制叶子节点的最大数量), 具体参数可用整数(20, 50)等, 优先调优最大深度, 此参数作为补充
        ccp_alpha=0, #剪枝强度参数
    )
    ```
    
- 模型拟合(训练)数据语句
    
    ```python
    DTR.fit(Xtrain,ytrain)
    ```
    
- 模型得分语句
    
    ```python
    #回归树返回R^2(决定系数)分数
    DTR.score(Xtrain,ytrain)
    DTR.score(Xtest,ytest)
    ```
    
- 模型预测语句
    
    ```python
    DTR.predict(Xtest) #模型返回各个样本的具体预测数值
    ```
    
- 查看各个特征对模型的重要性语句
    
    ```python
    DTC.feature_importances_
    ```
    

## 分类树

- 导入分类树模型语句:
    
    ```python
    from sklearn.tree import DecisionTreeClassifier as DTC
    ```
    
- 分类树模型参数详解
    
    ```python
    DTC = DTC(
        criterion='gini', #(默认基尼不纯度), 可选信息熵‘entropy’, (大多数分类任务首选‘gini’)
        splitter='best', #(默认‘best’), (常规场景默认‘best’, 大型数据集/防止过拟合选‘random’)
        max_depth=3, #(默认None: 表示树无限分裂直到信息不纯度为0或使用完所有训练集的特征—容易过拟合), 常用值3-10, 需要交叉验证和网格搜索调优参数
        max_features='sqrt', #(默认None: 表示最优分裂时考虑所有特征数量)(尤其是高维度特征数据集来限制特征数量), 可选‘sqrt’特征总数的平方根(分类常用)
        min_samples_leaf=10, #叶节点所需的最小样本数(即分裂到最后叶子节点时上一个节点至少要有指定数量的样本数量)默认为1(适合小数据集), 可调5-20(适合大数据集并防止过拟合)
        min_samples_split=10, #节点可分裂所需的最小样本数(即从上一个节点中至少有指定数量的样本数量才可以分裂), 默认为2(节点至少需2个样本才尝试分裂), 可调10-50(适合大数据集)
        min_impurity_decrease=0, #每次分裂要求必须降低的不纯度阈值, 一般默认即可
        random_state=42, #(默认None: 表示每次结果都随机), 指定随机种子数42, 以固定随机性
        
        #模型样本权重参数
        class_weight='balanced', #分类标签样本不均衡时, 先必选'balanced', 可进一步可自定义权重字典, 如: {0:1,1:2}
        min_weight_fraction_leaf=0, #叶节点样本权重和的最小比例, 除非样本权重不均, 否则不调
    
        #以上是重要参数, 以下是可选调参数
        
        max_leaf_nodes=None, #(默认None: 表示无限制叶子节点的最大数量), 具体参数可用整数(20, 50)等, 优先调优最大深度, 此参数作为补充
        ccp_alpha=0, #剪枝强度参数
    )
    ```
    
- 模型拟合(训练)数据语句
    
    ```python
    DTC.fit(Xtrain,ytrain)
    ```
    
- 模型得分语句
    
    ```python
    #分类树返回准确率Accuracy分数
    DTC.score(Xtrain,ytrain) 
    DTC.score(Xtest,ytest) 
    ```
    
- 模型预测语句
    
    ```python
    DTC.predict_proba(Xtest) #模型会返回预测各类标签的概率值
    ```
    
- 查看各个特征对模型的重要性语句
    
    ```python
    DTC.feature_importances_
    ```
    