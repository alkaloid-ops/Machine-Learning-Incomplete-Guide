
# 模型融合

## Voting

- 软投票: 训练多个基础模型, 将多个基础模型输出的预测值(回归)或概率值(分类)进行平均计算, 得出最终结果
- 硬投票: 训练多个基础模型, 将多个基础模型输出的预测类别标签(分类)进行计数, 少数服从多数类别标签, 得出最终结果
- 对于模型投票融合的加权平均方法中的权重需要结合超参数搜索(网格搜索或贝叶斯优化TPE)来确定最优权重参数
- 导入投票分类器语句
    
    ```python
    from mlxtend.classifier import EnsembleVoteClassifier
    ```
    
- 投票分类器使用参数
    
    ```python
    EnsembleVoteClassifier(
        clfs='实例化未训练的基模型', #多个模型以列表形式传入
        voting='soft', #硬投票基于标签, 软投票基与概率值
        weights=None, #每个基模型的权重
        verbose=0, #输出过程
        use_clones=True, #克隆基础模型
        fit_base_estimators=True #自动训练所有基模型
    )
    ```
    
- 拟合训练数据
    
    ```python
    EVC = EnsembleVoteClassifier().fit(Xtrain, ytrain)
    ```
    
- 融合模型预测语句
    
    ```python
    EVC.predict(Xtest)
    EVC.predict_proba(Xtest)
    ```
    

## Stacking

- 训练多个基础模型, 将多个基础模型的预测值(回归)或概率值(分类)作为训练数据, 输入一个新的元模型, 元模型的最终预测输出作为最终结果
- 或者以交叉验证的方式将多次交叉验证的结果作为新特征来训练元模型
- 元模型选择建议: 使用相对较为简单的模型(如线性回归/逻辑回归/贝叶斯回归/分类)
- 导入Stacking语句
    
    ```python
    from mlxtend.regressor import StackingRegressor #Stacking回归
    from mlxtend.regressor import StackingCVRegressor #Stacking交叉验证回归
    from mlxtend.classifier import StackingClassifier #Stacking分类
    from mlxtend.classifier import StackingCVClassifier #Stacking交叉验证分类
    ```
    
- Stacking参数详解
    
    ```python
    StackingRegressor(
        regressors='实例化未训练的基模型', #多个模型以列表形式传入
        meta_regressor='元模型',
        verbose=0, #输出过程
        use_features_in_secondary=False, #False元模型仅使用基模型的预测结果, True元模型使用基模型的预测结果+原始数据集特征
        store_train_meta_features=False, #保存训练过程中生成的元特征
        refit=True, #是否重新训练基模型
        multi_output=False #多输出(用于多目标回归)
    )
    
    StackingCVRegressor(
        regressors='实例化未训练的基模型', #多个模型以列表形式传入
        meta_regressor='元模型',
        cv=5, #交叉验证折数
        shuffle=True, #数据洗牌
        random_state=42,
        verbose=0, #输出过程
        refit=True, #是否重新训练基模型
        use_features_in_secondary=False, #False元模型仅使用基模型的预测结果, True元模型使用基模型的预测结果+原始数据集特征
        store_train_meta_features=False, #保存训练过程中生成的元特征
        n_jobs=-1, #计算线程数
        pre_dispatch='2*n_jobs', #控制并行任务分发
        multi_output=False #多输出(用于多目标回归)
    )
    
    StackingClassifier(
        classifiers='实例化未训练的基模型', #多个模型以列表形式传入
        meta_classifier='元模型',
        use_probas=False, #False使用预测类别作为元模型特征, True使用预测概率作为元模型特征
        drop_proba_col=None, #None保留所有类别的概率, last丢弃最后一列概率(use_probas=True时生效)
        average_probas=False, #False保留每个基分类器的完整概率向量, True对所有基分类器的概率取平均值
        verbose=0, #输出过程
        use_features_in_secondary=False, #False元模型仅使用基模型的预测结果, True元模型使用基模型的预测结果+原始数据集特征
        store_train_meta_features=False, #保存训练过程中生成的元特征
        use_clones=True, #克隆基础模型
        fit_base_estimators=True #自动训练所有基分类器
    )
    
    StackingCVClassifier(
        classifiers='实例化未训练的基模型', #多个模型以列表形式传入
        meta_classifier='元模型',
        cv=5, #交叉验证折数
        shuffle=True, #数据洗牌
        random_state=42，
        verbose=0, #输出过程
        use_probas==False, #False使用预测类别作为元模型特征, True使用预测概率作为元模型特征
        drop_proba_col=None, #None保留所有类别的概率, last丢弃最后一列概率(use_probas=True时生效)
        stratify=True, #分层抽样(保持各类别比例相同)
        use_features_in_secondary=False, #False元模型仅使用基模型的预测结果, True元模型使用基模型的预测结果+原始数据集特征
        store_train_meta_features=False, #保存训练过程中生成的元特征
        use_clones=True, #克隆基础模型
        n_jobs=-1, #计算线程数
        pre_dispatch='2*n_jobs', #控制并行任务分发
    )
    ```
    
- 拟合训练数据
    
    ```python
    SR = StackingRegressor().fit(Xtrain, ytrain)
    SCVR = StackingCVRegressor().fit(Xtrain, ytrain)
    SC = StackingClassifier().fit(Xtrain, ytrain)
    SCVC = StackingCVClassifier().fit(Xtrain, ytrain)
    ```
    
- 融合模型预测语句
    
    ```python
    SR.predict(Xtest)
    SR.predict_proba(Xtest)
    ```
    