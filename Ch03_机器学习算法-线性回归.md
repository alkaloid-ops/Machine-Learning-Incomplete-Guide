

# 线性回归

## 线性回归

- 线性回归函数
    
    $$
    \hat y =Xw+b
    $$
    

### 参数优化方法—最小二乘

- 从损失函数MSE出发

$$
MSE=J_{(w,b)}=\frac{1}{n}\sum^n_{i=1} (\hat y_i- y_i)^2=\sum^n_{i=1}(Xw-y)^2
$$

- 对参数求偏导数

$$
\frac{\partial J}{\partial w}=
\frac{\partial J}{\partial \hat y}\times 
\frac{\partial \hat y}{\partial w}=
2(Xw-y)\times X =
X^T(2Xw-2y)=
2X^TXw-2X^Ty=0
$$

$$
\Rightarrow 2X^TXw=2X^Ty\Rightarrow w = \frac{2X^Ty}{2X^TX}=(X^TX)^{-1}X^Ty
$$

### 参数优化方法—梯度下降

- 从损失函数MSE出发(2n是为了在求w,b偏导数时可以平方指数2抵消)
    
    $$
    MSE=J_{(w,b)}=\frac{1}{2n}\sum^n_{i=1} (\hat y_i- y_i)^2=\frac{1}{2n}\sum^n_{i=1}(Xw-y)^2
    $$
    
- 对参数求偏导数

$$
\frac{\partial J}{\partial w}=\frac{\partial J}{\partial \hat y}\times \frac{\partial \hat y}{\partial w}=\frac{1}{2n}\times 2(Xw-y)\times X =\frac{1}{n}X^T(Xw-y)
$$

$$
\Rightarrow w = w-\alpha \frac{1}{n}X^T(Xw-y)（参数w更新迭代公式）

$$

$$
\frac{\partial J}{\partial b}=\frac{\partial J}{\partial \hat y}\times \frac{\partial \hat y}{\partial b}=\frac{1}{2n}\times 2(Xw+b-y) =\frac{1}{n}(Xw-y)
$$

$$
\Rightarrow b = b-\alpha \frac{1}{n}(Xw-y)（参数b更新迭代公式）

$$

- 导入线性回归语句
    
    ```python
    from sklearn.linear_model import LinearRegression
    ```
    
- 线性回归参数详解
    
    ```python
    LinearRegression(
        fit_intercept=True, #拟合偏置b, 如果False就没有偏执b了
        copy_X=True, #创建副本
        n_jobs=-1, #-1为全部CPU核心计算, 1为单核
        positive=False #强制使权重系数为正值
    )
    ```
    
- 获取模型权重系数w
    
    ```python
    LR.coef_
    ```
    
- 获取模型偏置b
    
    ```python
    LR.intercept_
    ```
    

## Lasso套索回归(线性回归+L1正则化)

- 线性回归的损失函数上增加L1正则化与正则化系数(超参数lambda)
- 套索回归损失函数+L1正则化
    
    $$
    L(w,b)=\frac{1}{2n}\sum^n_{i=1} (\hat y_i- y_i)^2+\lambda\sum^n_{i=1}  {|w_i|}
    $$
    
- 套索回归损失函数的偏导数(正则化项不涉及偏置b)(正则项绝对值函数求导后为sign符号函数, 当w为正数时值为1, 当w为负数时值为-1, 当w为0时不可导)
    
    $$
    \frac{\delta L}{\delta w} = \frac{1}{n}X^T(Xw-y)+\lambda \cdot sign(w)
    $$
    
- 参数更新增加正则项
    
    $$
    w = w-\alpha [\frac{1}{n}X^T(Xw-y)+\lambda \cdot sign(w) ]= w-\alpha \frac{1}{n}X^T(Xw-y)-\alpha \lambda \cdot sign(w)
    $$
    
- 导入套索回归语句
    
    ```python
    from sklearn.linear_model import Lasso
    ```
    
- 套索回归参数详解
    
    ```python
    Lasso(
        alpha=1, #正则化系数-超参数
        fit_intercept=True, #拟合偏置b
        precompute=False, #是否预计算Gram矩阵(X^T@X)
        copy_X=True, #创建数据副本
        max_iter=1000, #迭代次数(学习率/迭代步长)-超参数
        tol=0.0001, #每次误差减少的阈值, 小于这个值时, 停止迭代
        warm_start=False, #热启动, 使用上一次的参数
        positive=False, #是否权重系数都为正数
        random_state=42,
        selection='cyclic' #特征选择策略, cyclic按照特征顺序选择, random随机选择-大数据
    )
    ```
    
- 获取模型权重系数w
    
    ```python
    Lasso.coef_
    ```
    
- 导入套索回归交叉验证语句(用于确定最优正则化参数)
    
    ```python
    from sklearn.linear_model import LassoCV
    ```
    
- 套索回归交叉验证参数详解
    
    ```python
    LassoCV(
        eps=0.001, #正则化路径的计算精度
        n_alphas='deprecated', 
        alphas='warn', #正则化参数的候选值列表(np.logspace(-4,0,50)#10^-4到10^0之间50个值)
        fit_intercept=True, #拟合偏置b
        precompute='auto', #是否预计算Gram矩阵(X^T@X)
        max_iter=1000, #迭代次数(学习率/迭代步长)-超参数
        tol=0.0001, #每次误差减少的阈值, 小于这个值时, 停止迭代
        copy_X=True, #创建数据副本
        cv=None, #默认None为5折
        verbose=False, #输出进度
        n_jobs=-1, #计算核心数
        positive=False, #是否权重系数都为正数
        random_state=42,
        selection='cyclic' #特征选择策略, cyclic按照特征顺序选择, random随机选择-大数据
    )
    ```
    
- 获取交叉验证后的最优正则化参数
    
    ```python
    LassoCV.alpha_
    ```
    
- 获取各个正则化系数取值下交叉验证均方误差
    
    ```python
    LassoCV.mse_path_
    ```
    

## Ridge岭回归(线性回归+L2正则化)

- 线性回归的损失函数上增加L2正则化与正则化系数(超参数lambda)
- 岭回归损失函数+L2正则化
    
    $$
    L(w,b)=\frac{1}{2n}\sum^n_{i=1} (\hat y_i- y_i)^2+\frac{\lambda}{2n} \sum^n_{i=1}  w_i^2
    $$
    
- 岭回归损失函数的偏导数(正则化项不涉及偏置b)
    
    $$
    \frac{\delta L}{\delta w} = \frac{1}{n}X^T(Xw-y)+\frac{\lambda}{n}w
    $$
    
- 参数更新增加正则项
    
    $$
    w = w-\alpha [\frac{1}{n}X^T(Xw-y)+\frac{\lambda}{n}w ]= w-\alpha \frac{1}{n}X^T(Xw-y)-\alpha \frac{\lambda}{n}w
    $$
    
- 导入岭回归语句
    
    ```python
    from sklearn.linear_model import Ridge
    ```
    
- 岭回归参数详解
    
    ```python
    Ridge(
        alpha=1, #正则化系数-超参数
        fit_intercept=True, #拟合偏置b
        copy_X=True, #创建数据副本
        max_iter=None, #迭代次数(学习率/迭代步长)-超参数
        tol=0.0001, #每次误差减少的阈值, 小于这个值时, 停止迭代
        solver='auto', #求解算法, 备选参数lsqr迭代法-梯度下降, cholesky标准求解-最小二乘
        positive=False, #是否权重系数都为正数
        random_state=42
    )
    ```
    
- 导入岭回归交叉验证语句(用于确定最优正则化参数)
    
    ```python
    from sklearn.linear_model import RidgeCV
    ```
    
- 岭回归交叉验证参数详解
    
    ```python
    RidgeCV(
        alphas='给定正则化参数的元组',
        fit_intercept=True, #拟合偏置b
        scoring='r2', #评估指标, 备选参数'neg_mean_squared_error'
        cv=None, #默认None使用留一法, 可指定具体折数
        gcv_mode='auto', #当cv=None时使用, 备选参数svd矩阵分解, eigen特征值分解
        store_cv_values=False, #cv=None时使用, 存储每个交叉验证值
        alpha_per_target=False #多任务的每个alpha
    )
    ```
    
- 获取交叉验证后的最优正则化参数
    
    ```python
    RidgeCV.alpha_
    ```
    
- 获取留一法的每个交叉验证值
    
    ```python
    RidgeCV.cv_values_ #当cv=None和store_cv_values=True才能使用
    ```
    

## ElasticNet弹性网回归(线性回归+L1/L2正则化)

- 导入弹性网回归语句
    
    ```python
    from sklearn.linear_model import ElasticNet
    from sklearn.linear_model import ElasticNetCV #交叉验证
    ```
    
- 弹性网回归参数详解
    
    ```python
    ElasticNet(
        alpha=1, #正则化系数超参数
        l1_ratio=0.5, #混合l1l2双正则化的比例
        fit_intercept=True, #计算偏置b
        precompute=False, #是否预计算Gram矩阵(特征矩阵内积自身转置)
        max_iter=1000, #最大迭代次数
        copy_X=True, #复制训练集
        tol=0.0001, #收敛阈值
        warm_start=False, #热启动
        positive=False, #强制正系数
        random_state=42,
        selection='cyclic' #顺序更新特征系数, 备选random随机更新特征系数(高维数据)
    )
    ```
    
- 拟合训练数据
    
    ```python
    ElasticNet().fit(Xtrain, ytrain)
    ```
    
- 弹性网回归交叉验证参数详解
    
    ```python
    ElasticNetCV(
        l1_ratio=0.5, #混合l1l2双正则化的比例
        eps=0.001, #定义alpha最小值
        n_alphas=100, #范围内生成的alpha数量
        alpha=None, #提供自定义超参数数组
        fit_intercept=True, #计算偏置b
        precompute=False, #是否预计算Gram矩阵(特征矩阵内积自身转置)
        max_iter=1000, #最大迭代次数
        tol=0.0001, #收敛阈值
        cv=5, #交叉验证折数
        copy_X=True, #复制训练集
        verbose=0, #过程输出
        n_jobs=-1, #计算线程数
        positive=False, #强制正系数
        random_state=42,
        selection='cyclic' #顺序更新特征系数, 备选random随机更新特征系数(高维数据)
    )
    ```
    