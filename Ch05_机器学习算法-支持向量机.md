
# 支持向量机

- 支持向量机的支持向量: 支持向量是离决策边界(中心分割线)最近的数据点, 这些数据点就是支持向量, 点构成线并与中心分割线平行构成最大间隔区域
- 使用支持向量机分类前推荐将数据无量纲化
- 向量机模型会将预测分类标签(0和1)自动转换为-1和1来拟合训练, 最终预测时仍会还原标签(0和1)
- 二分类线性SVC模型决策函数(模型函数输出为大于0为正样本, 小于0为负样本—正负符号判断)

$$
\hat y = sign(w^Tx+b)（符号函数输出预测值为-1或1）
$$

- 合页损失函数

$$
L = \sum^n_{i=1} max(0,1-y_i(w^Tx_i+b))
$$

- 核函数表达式(用作将数据点投影到高维空间使数据点可分类)
    
$$
Linear = K(x@y)\quad Poly=K(\gamma(x@y)+r)^d\quad Sigmoid=K(tanh(\gamma (x@y)+r))\quad RBF=K(e^{-\gamma ||x-y||^2})(\gamma>0)
$$
    

## 硬间隔

- 数据几乎可以完美的分割成两类(最大间隔内无数据点)
- 增加正则项(硬间隔)的合页损失函数

$$
L = \frac{1}{2} \|w\|^2+\sum^n_{i=1} max(0,1-y_i(w^Tx_i+b))
$$

- 损失函数优化目标:
    - 正则项: 权重L2范数的平方(二分之一只是为了方便求导)使达到最大化间隔, 间隔中心为决策边界
    - 正则化系数C: 用来权衡最大化间隔和损失项(C值大更注重损失, 即模型关注分类精度, 模型易过拟合; C值小更注重最大化间隔, 增加容错, 模型易欠拟合)
    - 损失项: 如果模型决策函数将样本(间隔外)预测正确, 那么max函数内部会是(0, 1-1*1)输出值为0(即损失为0)
    - 损失项: 如果模型决策函数将样本(间隔内)预测正确, 那么max函数内部会是(0, 1-1*决策函数具体值)输出值为大于0小于1的值(即略有损失)
    - 损失项: 如果模型决策函数将样本预测错误, 那么max函数内部会是(0,1-1*-1)输出值为2(即损失为2)(如果模型频繁预测错误那么损失会很大)
    - 损失函数更关注靠近决策边界的样本, 不关心离决策边界遥远的异常值(模型更鲁棒)
- 优化目标(求解能让损失最小化的w和b参数, 同时带有不等式的约束条件)

$$
\min\limits_{w,b} \frac{1}{2} \|w\|^2+\sum^n_{i=1} max(0,1-y_i(w^Tx_i+b))\quad y_i(w^Tx_i+b)\geq 1
$$

$$
（当y_i(w^Tx_i+b)\geq 1时, 才能让1-y_i(w^Tx_i+b)为0或负数, 从而使得最大值为0, 以至于总损失无限逼近0）
$$

- 构建拉格朗日函数 (拉格朗日乘子与约束条件相乘后, 减去大于等于的条件或加上小于等于的条件)

$$
L = \frac{1}{2} \|w\|^2+\sum^n_{i=1} \alpha_i (1-y_i(w^Tx_i+b))\quad \alpha_i \geq 0
$$

$$
=\quad \frac{1}{2} \|w\|^2+\sum^n_{i=1} \alpha_i -\sum^n_{i=1}\alpha_i(y_i(w^Tx_i+b))\quad \alpha_i \geq 0\quad =\frac{1}{2} \|w\|^2+\sum^n_{i=1} \alpha_i -\sum^n_{i=1}\alpha_iy_iw^Tx_i -\sum^n_{i=1}\alpha_i y_i b \quad \alpha_i \geq 0
$$

- 分别对w和b求偏导数

$$
\frac{\partial L}{\partial w}=w-\sum^n_{j=1}\alpha _j y_j x_j=0\Rightarrow w=\sum^n_{j=1}\alpha _j y_j x_j
$$

$$
\frac{\partial L}{\partial b}=
-\sum^n_{i=1}\alpha_i y_i =0
\Rightarrow
\sum^n_{i=1}\alpha_i y_i
$$

- 转换对偶函数 (将w的偏导数代入拉格朗日函数以替换掉w)

$$
L=\frac{1}{2} (\sum^n_{j=1}\alpha_j y_j x_j)^2+\sum^n_{i=1} \alpha_i -\sum^n_{i,j=1}\alpha_i y_i \alpha _j y_j x_i^Tx_j
=\frac{1}{2}(\sum^n_{i=1}\alpha_iy_ix_i \times \sum^n_{i=1}\alpha_jy_jx_j)+\sum^n_{i=1} \alpha_i -\sum^n_{i,j=1}\alpha_i\alpha_jy_iy_jx_i^Tx_j
$$

$$
=\frac{1}{2}\sum^n_{i=1}\alpha_i\alpha_jy_iy_jx_i^Tx_j+\sum^n_{i=1} \alpha_i -\sum^n_{i,j=1}\alpha_i\alpha_jy_iy_jx_i^Tx_j=\sum^n_{i=1} \alpha_i -\frac{1}{2}\sum^n_{i,j=1}\alpha_i\alpha_jy_iy_jx_i^Tx_j
$$

$$
最终优化目标：\max\limits_{\alpha}\sum^n_{i=1} \alpha_i -\frac{1}{2}\sum^n_{i,j=1}\alpha_i\alpha_jy_iy_jx_i^Tx_j （约束条件：\alpha_i \geq 0\quad \sum^n_{i=1}\alpha_i y_i=0）
$$

## 软间隔

- 当不同类的数据离的较近甚至有交织, 那么将很难完全分离开, 引入松弛系数以及C正则化系数(惩罚系数)来放松约束条件, 允许一些样本被错误分类或落在间隔内(而这个最大间隔会被调整—扩大)
- 软间隔引入惩罚系数C和松弛变量xi构建拉格朗日函数

$$
L = \frac{1}{2} \|w\|^2+C\sum^n_{i=1}\xi_i -\sum^n_{i=1} \alpha_i (y_i(w^Tx_i+b)-1+\xi_i)-\sum^n_{i=1} \mu_i \xi_i
$$

$$
= \frac{1}{2} \|w\|^2+C\sum^n_{i=1}\xi_i -\sum^n_{i=1}\alpha_iy_iw^Tx_i-\sum^n_{i=1}\alpha_iy_ib+\sum^n_{i=1}\alpha_i-\sum^n_{i=1}\alpha_i\xi_i-\sum^n_{i=1} \mu_i \xi_i
$$

$$
\quad约束条件： y_i(w^Tx_i+b)\geq 1-\xi_i \quad \xi\geq0
$$

- 对参数（w，b，xi）求偏导数

$$
\frac{\partial L}{\partial w}=
w-\sum^n_{j=1}\alpha_jy_jx_j
\Rightarrow
w=\sum^n_{j=1}\alpha_jy_jx_j
$$

$$
\frac{\partial L}{\partial b}=
-\sum^n_{i=1}\alpha_i y_i =0
\Rightarrow
\sum^n_{i=1}\alpha_i y_i
$$

$$
\frac{\partial L}{\partial \xi}=
C-\alpha_i-\mu_i=0
\Rightarrow
C=\alpha_i+\mu_i
$$

- 转换对偶函数 (将w和C的偏导数代入拉格朗日函数以替换掉w)

$$
L=\frac{1}{2}(\sum^n_{j=1} \alpha_jy_jx_j)^2+(\alpha_i+\mu_i)\sum^n_{i=1}\xi_i-\sum^n_{i=1}\alpha_i\alpha_jy_iy_jx_i^Tx_j+\sum^n_{i=1}\alpha_i-\sum^n_{i=1}\alpha_i\xi_i-\sum^n_{i=1}\mu_i\xi_i
$$

$$
=\frac{1}{2}\sum^n_{i=1}
\alpha_i\alpha_jy_iy_jx_i^Tx_j+\sum^n_{i=1} \alpha_i -\sum^n_{i,j=1}\alpha_i\alpha_jy_iy_jx_i^Tx_j
=\sum^n_{i=1} \alpha_i -\frac{1}{2}\sum^n_{i,j=1}\alpha_i\alpha_jy_iy_jx_i^Tx_j
$$

$$
（其中(\alpha_i+\mu_i)\sum^n_{i=1}\xi_i=\sum^n_{i=1}\alpha_i\xi_i+\sum^n_{i=1}\mu_i\xi_i与最后两项-\sum^n_{i=1}\alpha_i\xi_i-\sum^n_{i=1}\mu_i\xi_i抵消，其余项和硬间隔一致）
$$

$$
最终优化目标：\max\limits_{\alpha}\sum^n_{i=1} \alpha_i -\frac{1}{2}\sum^n_{i,j=1}\alpha_i\alpha_jy_iy_jx_i^Tx_j （约束条件：\alpha_i \geq 0\quad \sum^n_{i=1}\alpha_i y_i=0\quad 0\leq a_i\leq C）
$$

- KKT条件

$$
平稳性(对于损失函数中参数的偏导数为0)
$$

$$
原始可行性(y_i(w^Tx_i+b)\geq 1-\xi_i\quad \xi_i\geq0)
$$

$$
对偶可行性(\alpha_i ,\mu_i \geq 0)(拉格朗日乘子都要大于等于0)
$$

$$
互补松弛性(\alpha_i (y_i(w^Tx_i+b)-1+\xi)=0\quad \mu_i\xi_i=0\quad \alpha_i+\mu_i=C)
$$

- 求解乘子alpha(将w替换为偏导数)

$$
\hat y = sign(w^Tx+b)=sign(\sum^n_{i=1}\alpha_iy_ix_i^Tx_i+b)
$$

- 最后求解b

$$
y_i(w^Tx_i+b)= 1
\Rightarrow
y_i(\sum^n_{i,j=1}a_jy_jx_j^Tx_i+b)= 1
\Rightarrow
y_ib=1-y_i(\sum^n_{i,j=1}a_jy_jx_j^Tx_i)
\Rightarrow
b=y_i-\sum^n_{i,j=1}a_jy_jx_j^Tx_i
$$

## 支持向量回归SVR

- 导入支持向量回归语句
    
    ```python
    from sklearn.svm import SVR
    ```
    
- 支持向量回归参数详解
    
    ```python
    SVR(
        kernel='rbf', #核函数(将数据映射到高维空间使其可非线性分类), 备选参数: 'linear'线性核(线性可分数据), 'rbf'高斯核(非线性可分数据), 'poly'多项式核(配合degree指定阶数), 'sigmoid'sigmoid核(类似神经网络激活函数)
        degree=3, #多项式核指定阶数(只有kernel='poly')(阶数过高易过拟合)
        gamma='scale', #核函数系数(仅对高斯核、多项式核、sigmoid核有效), scale为特征数*特征矩阵的方差的倒数, auto为特征数的倒数, 可自定义大于0的浮点数(值越大决策边界曲折-会过拟合, 值越小决策边界平滑-会欠拟合)
        coef0=0, #影响多项式核和sigmoid核的高阶项权重(核函数表达式中的r)
        tol=0.001, #优化算法的停止容差(每次优化的误差减少值)
        C=1, #正则化系数, C越大分类严格, C越小分类宽松
        epsilon=0.1, #容忍带宽度(约为目标值标准差的10%)
        shrinking=True, #是否使用收缩启发式算法以加速优化过程
        cache_size=200, #核矩阵缓存大小(MB)(加速核函数计算)
        verbose=False, #输出计算过程
        max_iter=-1, #最大迭代次数, 默认-1为无限制
    )
    ```
    
- 拟合训练数据
    
    ```python
    SVR = SVR().fit(Xtrain, ytrain)
    ```
    
- 获取模型预测结果
    
    ```python
    SVR.predict(Xtest)
    ```
    
- 获取支持向量的坐标数组
    
    ```python
    SVR.support_vectors_
    ```
    
- 获取各个类别的支持向量个数
    
    ```python
    SVR.n_support_
    ```
    
- 获取每个特征的重要性
    
    ```python
    SVR._coef_ #仅适用于kernel='linear'
    ```
    
- 获取决策边界的偏置b
    
    ```python
    SVR.intercept_
    ```
    
- 获取拉格朗日乘子
    
    ```python
    SVR.dual_coef_
    ```
    

## 支持向量分类SVC

- 导入支持向量分类语句
    
    ```python
    from sklearn.svm import SVC
    ```
    
- 支持向量分类参数详解
    
    ```python
    SVC(
        C=1, #正则化系数, C越大分类严格, C越小分类宽松
        kernel='rbf', #核函数(将数据映射到高维空间使其可非线性分类), 备选参数: 'linear'线性核(线性可分数据), 'rbf'高斯核(非线性可分数据), 'poly'多项式核(配合degree指定阶数), 'sigmoid'sigmoid核(类似神经网络激活函数)
        degree=3, #多项式核指定阶数(只有kernel='poly')(阶数过高易过拟合)
        gamma='scale', #核函数系数(仅对高斯核、多项式核、sigmoid核有效), scale为特征数*特征矩阵的方差的倒数, auto为特征数的倒数, 可自定义大于0的浮点数(值越大决策边界曲折-会过拟合, 值越小决策边界平滑-会欠拟合)
        coef0=0, #影响多项式核和sigmoid核的高阶项权重(核函数表达式中的r)
        shrinking=True, #是否启用启发式收缩(加速求解过程的启发式方法)
        probability=False, #是否启用概率估计(训练后会额外计算类别概率)
        tol=0.001, #优化算法的停止容差(每次优化的误差减少值)
        cache_size=200, #核矩阵缓存大小(MB)(加速核函数计算)
        class_weight=None, #类别权重
        verbose=False, #输出计算过程
        max_iter=-1, #最大迭代次数, 默认-1为无限制
        decision_function_shape='ovr', #多分类策略
        break_ties=False, #打破预测平局(是否通过额外计算确定最终类别)(当出现各类别得分一致的)
        random_state=42
    )
    #多数场景下使用高斯核rbf, 使用网格搜索优化C和gamma参数
    ```
    
- 拟合训练数据
    
    ```python
    SVC = SVC().fit(Xtrain, ytrain)
    ```
    
- 获取模型分类预测结果(是类别编码不是概率值)(需要概率值需要在模型实例化时配置参数probability=True)
    
    ```python
    SVC.predict(Xtest)
    SVC.predict_proba(Xtest) #输出类似逻辑回归的概率
    ```
    
- 获取支持向量的坐标数组
    
    ```python
    SVC.support_vectors_
    ```
    
- 获取各个类别的支持向量个数
    
    ```python
    SVC.n_support_
    ```
    
- 获取每个特征的重要性
    
    ```python
    SVC._coef_ #仅适用于kernel='linear'
    ```
    
- 获取决策边界的偏置b
    
    ```python
    SVC.intercept_
    ```
    
- 获取拉格朗日乘子
    
    ```python
    SVC.dual_coef_
    ```
    
