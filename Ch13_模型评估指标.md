
# 模型评估

## 回归指标

- MSE均方误差公式(每个样本的模型预测值减去对应样本的真实值的平方和的均值)
    
    $$
    MSE=\frac{1}{n} \sum ^n_{i=1} ({\hat{y_{i}}}-y_i )^2
    $$
    
- RMSE均方根误差公式(在MSE的基础上再求平方根)
    
    $$
    RMSE=\sqrt {MSE} = \sqrt {\frac{1}{n} \sum ^n_{i=1} ({\hat{y_{i}}}-y_i )^2}
    $$
    
- R^2决定系数公式(其中分子是预测值与真实值的误差的平方和, 分母是真实值与真实值均值的差的平方和)
    
    $$
    R^2 =1- \frac{\sum^n_{i=1}(y_i-\hat y_i)^2}{\sum^n_{i=1}(y_i-\bar y_i)^2}
    $$
    
- MAE平均绝对误差(每个样本的模型预测值减去对应样本的真实值的绝对值和的均值)(不常用)
    
    $$
    MAE = \frac{1}{n} \sum ^n_{i=1} | {\hat{y_{i}}}-y_i |
    $$
    
- 导入MSE语句
    
    ```python
    from sklearn.metrics import mean_squared_error as MSE
    ```
    
- MSE参数详解
    
    ```python
    MSE(
        y_true='真实值',
        y_pred='预测值',
        sample_weight=None, #参数类型为数组或列表, 每个样本的权重
        multioutput='uniform_average', #默认值; 当回归问题有多个预测值时使用'raw_values'
        squared=True #True输出均方误差MSE, False输出均方根误差RMSE
    )
    ```
    
- 计算R^2决定系数语句(如果R^2分数为负数, 先检查数据预处理、特征工程、建模方式是否有问题, 若没问题说明模型无法拟合数据(数据毫无规律))
    
    ```python
    #模型实例化名.score(ytest, ytrain)
    ```
    

## 分类指标

### 混淆矩阵

- 混淆矩阵记录模型分类预测标签与真实标签的正确与错误的计数
- 混淆矩阵是个方阵, 二分类任务时, 混淆矩阵为2x2; 三分类任务时, 混淆矩阵为3x3, 多分类时, 混淆矩阵为NxN
- 二分类混淆矩阵(其中TP和TN是模型预测正确的正样本与负样本数; FN(错误的负样本)是模型将正样本误判为负样本; FP(错误的正样本)是模型将负样本误判为正样本)
    
    $$
    \begin{bmatrix} TP & FN\\ FP & TN \end{bmatrix}
    $$
    

### 准确率Accuracy

- 模型预测正确的样本数量占全部样本数量的比例
- 基于混淆矩阵的计算公式
    
    $$
    Accuracy = \frac{TP+TN}{TP+TN+FP+FN}
    $$
    

### 精确率Precision

- 模型预测某一标签正确的数量占模型预测这个标签全部数量的比例(单一标签下计算)
- 当模型预测错误代价高昂时, 追求高精准率(例如模型预测异常样本数20, 其中有18个正确, 有2个误判, 模型要尽可能减少误判的样本)
- 提高模型预测概率值的阈值可提高精确率
- 基于混淆矩阵的计算公式(矩阵垂直方向计算)
    
    $$
    Precision = \frac{TP}{TP+FP}
    $$
    

### 召回率Recall

- 模型预测某一标签正确的数量占这个标签全部数量的比例
- 当模型预测预测不全少数类样本时, 追求高召回率(例如模型预测异常样本数30, 总异常样本数25, 有5个误判, 模型宁可误判, 但绝不能漏)
- 降低模型预测概率值的阈值可提高召回率
- 误判率=1-召回率(即被误判的占比)
- 基于混淆矩阵的计算公式(矩阵水平方向计算)
    
    $$
    Recall = \frac{TP}{TP+FN}
    $$
    

### F1-Score

- 均衡精确率和召回率的指标
- 计算公式
    
    $$
    F1-Score = 2\times\frac{ Precision \times Recall}{Precision +Recall}
    $$
    

### 特异度Specificity与假正率FPR

- 特异度是模型预测多数类标签正确的数量占这个多数类标签总数量的比例(衡量模型对多数类预测的正确率)
- 基于混淆矩阵的计算公式(矩阵水平方向计算)
    
    $$
    Specificity = \frac{TN}{FP+TN}
    $$
    
- 假正率FPR=1-Specificity(衡量模型对多数类预测的错误率)

### ROC曲线与AUC面积

- ROC曲线表示模型在不同阈值下的表现
- 曲线图像解读: 横轴为假正率FPR(模型对负样本预测错误的比例)(越小越好), 纵轴为召回率Recall(模型对正样本预测正确的比例)(越大越好)
- 曲线从陡峭变平缓的拐点是最佳分类阈值点(该点平衡了FPR与Recall)
- 计算拐点对应的阈值(Recall值较高同时FPR值较低, 即两者的差值最大时)
- 根据这个差值在ROC曲线函数返回的数组中找到索引, 并使用这个索引在阈值数组中找到对应的阈值取值
    
    $$
    max(Recall-FPR)
    $$
    
- AUC面积是ROC曲线下的面积
- AUC面积值在0.9以上为优秀, 0.8-0.9之间为良好, 0.7-0.8之间为一般, 0.6-0.7之间为较差

### classification_report分类模型评估报告

- 将输出一个分类模型评估报告
    - 报告各列名为精准率、召回率、F1-Score、Support(该类真实样本数)
    - 报告行为按照类别标签(如三类标签为三行)
    - 报告最后三行为准确率、宏观平均(所有类别指标的平均值)、加权平均(所有类别指标的加权平均值)
- 导入分类模型评估报告语句
    
    ```python
    from sklearn.metrics import classification_report
    ```
    
- 分类模型评估报告参数详解
    
    ```python
    classification_report(
        y_true='真实标签',
        y_pred='预测标签',
        labels='标签顺序', #参数类型为数组或列表
        target_names='类别名称', #参数类型为数组或列表
        sample_weight=None, #参数类型为数组或列表, 每个样本的权重
        digits='保留小数位数',
        output_dict=False, #默认False返回格式化字符串, True为返回字典(可读性不好)
        zero_division='warn' #除0处理, 默认warn返回警告和0
    )
    ```
    

### 布里尔分数

- 用作概率质量评估(如当阈值默认0.5时, 模型预测为正样本的概率值高出阈值一点还是很多, 无论是0.6还是0.9都大于阈值)
- 布里尔分数公式(其中P为预测概率, O为真实标签数值)(分数越接近0越好)(衡量单一模型)

$$
BrierScore=\frac{1}{n}\sum^n_{i=1} (P_i-O_i)^2
$$

- 导入布里尔分数语句
    
    ```python
    from sklearn.metrics import brier_score_loss
    ```
    
- 布里尔分数参数详解
    
    ```python
    brier_score_loss(
        y_true='真实标签',
        y_pred='预测标签',
        sample_weight=None, #参数类型为数组或列表, 每个样本的权重
        pos_label='指定样本名' #多分类时指定计算某一个类别的分数
    )
    ```
    

### 对数损失函数

- 对数损失函数公式
    
    $$
    LogLoss = -\frac{1}{n}\sum^n_{i=1} [y_ilog(\hat y_i)+(1-y_i)log(1-\hat y_i)]
    $$
    
- 导入对数损失函数语句
    
    ```python
    from sklearn.metrics import log_loss
    ```
    
- 对数损失函数参数详解
    
    ```python
    log_loss(
        y_true='真实标签',
        y_pred='预测标签',
        sample_weight=None, #参数类型为数组或列表, 每个样本的权重
        eps=1e-15, #概率截断值, 避免数值溢出(因log(0)=∞)
        normalize=True, #返回损失均值, False返回损失总和
        labels=None #多分类时需固定类别顺序
    )
    ```
    

### 可靠性曲线

- 可靠性曲线用于评估分类模型预测概率的校准质量（二分类中好的概率质量应两极分化）
- 将真实标签进行分箱, 计算每个箱内的正样本的比例, 与模型的预测概率值构成二维可视化图
- 导入可靠性曲线语句
    
    ```python
    from sklearn.calibration import calibration_curve
    ```
    
- 可靠性曲线参数详解
    
    ```python
    calibration_curve(
        y_true='真实标签',
        y_score='预测概率', 
        pos_label='指定哪个标签作为正类', #输入为字符串类别名或整数类别编码
        normalize=False, #归一化概率区间
        n_bins=5, #分箱数量(将预测概率离散化为若干区间)
        strategy='uniform' #分箱策略(uniform等宽分箱-箱子的极差一致, quantile等频分箱-箱子样本数一致)
    )
    ```
    
- 函数返回两个数组(第一个数组为每个分箱中真实正类的比例, 第二个数组为每个分箱的预测概率均值)(将第一个数组作为纵轴, 第二个数组为横轴可视化, 理想图像为两者数值接近-即接近对角线)(但不能完全是直线)

### 概率校准

- 概率校准关注概率可靠性而非分类能力(当业务关注于是否能正确分类就无需校准概率, 当业务关注于具体的概率值时需要校准)
- 导入概率校准语句
    
    ```python
    from sklearn.calibration import CalibratedClassifierCV
    ```
    
- 概率校准参数详解
    
    ```python
    CalibratedClassifierCV(
        estimator='模型名',
        method='sigmoid', #中小数据集用sigmoid, 大数据集用isotonic
        cv=5 #交叉验证, 默认5折, 备选'prefit'模型已经拟合数据训练过了, 就直接选prefit
        n_jobs=-1, #计算单元
        ensemble=True #每折的平均值
    )
    ```
    
- 导入各个指标语句
    
    ```python
    from sklearn.metrics import confusion_matrix #混淆矩阵
    from sklearn.metrics import accuracy_score #准确率
    from sklearn.metrics import precision_score #精准率
    from sklearn.metrics import recall_score #召回率
    from sklearn.metrics import f1_score #F1-Score(精准率与召回率的均衡)
    from sklearn.metrics import precision_recall_curve #精确率-召回率曲线
    from sklearn.metrics import roc_curve #ROC曲线
    from sklearn.metrics import roc_auc_score #AUC面积
    ```
    
- 各个指标参数详解
    
    ```python
    #混淆矩阵
    confusion_matrix(
        y_true='真实标签',
        y_pred='预测标签',
        labels='类别名', #参数类型为数组或列表, 二分类为2列, 多分类为多列(按标签数值编码降序传入)
        sample_weight=None, #参数类型为数组或列表, 每个样本的权重
        normalize=None, #混淆矩阵的数值归一化, 归一化后数值为比例, 不再是计数了
    )
    
    #准确率
    accuracy_score(
        y_true='真实标签',
        y_pred='预测标签',
        normalize=None, #混淆矩阵的数值归一化, 归一化后数值为比例, 不再是计数了
        sample_weight=None, #参数类型为数组或列表, 每个样本的权重
    )
    
    #精准率/召回率/f1-score
    precision_score/recall_score/f1_score(
        y_true='真实标签',
        y_pred='预测标签',
        labels='类别名', #参数类型为数组或列表, 二分类为2列, 多分类为多列(按标签数值编码降序传入)
        pos_label='指定正样本名', #多分类时指定计算某一个类别的分数
        sample_weight=None, #参数类型为数组或列表, 每个样本的权重
        average='指定多分类的计算方式' #默认'binary'二分类, 'micro'全局计算(全部类先求和再算比例), 'macro'各类别指标值的均值, None返回各类别的指标值
        zero_division='warn' #除0处理, 默认warn返回警告和0
    )
    
    #精确率-召回率曲线(计算在不同阈值下的精准率和召回率)(precision_recall_curve在样本标签高度不均衡数据时使用)
    precision_recall_curve(
        y_true='真实标签',
        probas_pred='模型预测的正类概率值' 
        pos_label='指定正样本名', #多分类时指定计算某一个类别的分数
        sample_weight=None, #参数类型为数组或列表, 每个样本的权重
    )
    
    #ROC曲线
    roc_curve(
        y_true='真实标签',
        y_score='预测概率值',
        sample_weight=None, #参数类型为数组或列表, 每个样本的权重
        pos_label='指定正样本名', #多分类时指定计算某一个类别的分数
        drop_intermediate=True #删除不会改变ROC曲线的中间阈值点
    )
    #返回三个数组, 第一个数组为FPR随着阈值变化的值, 第二个数组为Recall随着阈值变化的值, 第三个数组为阈值取值数组
    
    #AUC面积
    roc_auc_score(
        y_true='真实标签',
        y_score='预测概率值',
        average='指定多分类的计算方式' #默认'binary'二分类, 'macro'各类别指标值的均值, 'weighted'各类别指标值的加权平均值, None返回各类别的指标值
        sample_weight=None, #参数类型为数组或列表, 每个样本的权重
        max_fpr=None, #计算完整AUC, 可传入0-1之间的浮点数表示只计算区间AUC
        multi_class='ovr', #多分类策略, ovr为每个类别单独计算AUC, ovo为每对类别计算AUC后平均
        labels='类别名', #参数类型为数组或列表, 二分类为2列, 多分类为多列(按标签数值编码降序传入)
    )
    ```
    

## 聚类指标

### silhouette_score轮廓系数

- 量化样本在所属簇中的紧密性(相似度a)和与其他簇的分离度(离散性b)(不需要真实标签)
- 单个样本轮廓系数计算公式
    
    $$
    S = \frac{b-a}{max(a,b)}
    $$
    
- 所有样本轮廓系数为所有单个样本轮廓系数的均值
- 计算所有样本轮廓系数语句
    
    ```python
    from sklearn.metrics import silhouette_score
    ss = silhouette_score(X, ypred)
    ```
    
- 返回值在-1至1之间, 负值表示样本在同簇相似度低, 与其他簇距离较近(归类错误), 接近0值为样本位于簇与簇之间的边界, 分数大于0且越接近1越好
- 获取单个样本轮廓系数语句
    
    ```python
    from sklearn.metrics import silhouette_samples
    ss = silhouette_samples(X, ypred)
    #返回一个一维数组, 每个数值为每个样本的轮廓系数
    ```
    

### adjusted_rand_score调整兰德系数

- 需要有真实标签才能计算
- 导入调整兰德系数语句
    
    ```python
    from sklearn.metrics import  adjusted_rand_score
    ```
    
- 计算调整兰德系数语句
    
    ```python
    ars = adjusted_rand_score(ylabel, ypred)
    ```
    
- 返回值在-1至1之间, 负值表示簇内点差异巨大, 接近0值为聚类与随机匹配效果差不多, 分数大于0且越接近1越好

### calinski_harabasz_score方差比准则

- 方差比准则计算公式(其中n为数据样本数, k为聚类质心数, tr(B_k)为不同簇之间的协方差矩阵的迹, tr(W_k)为簇内协方差矩阵的迹)
    
    $$
    chs = \frac{tr(B_k)}{tr(W_k)} \times \frac{n-k}{k-1}
    $$
    
- 计算方差比准则系数语句
    
    ```python
    from sklearn.metrics import calinski_harabasz_score
    chs = calinski_harabasz_score(X, ypred)
    ```
    
- 追求系数越大越好

## 交叉验证

- 导入K折交叉验证语句
- 将训练数据等分为k, 轮流取其中一份为验证集, 剩余份为训练集
    
    ```python
    from sklearn.model_selection import KFold, StratifiedKFold, GroupKFold
    #标准K折
    KF = KFold(n_splits=k, shuffle=True, random_state=42) #k值常取5-10
    #分层K折(分类任务中类别不均衡使用, 使其保持类别比例)
    KF = StratifiedKFold(n_splits=5, shuffle=True, random_state=42)
    #分组K折
    KF = GroupKFold(n_splits=5)
    ```
    
- 交叉验证返回单个评估指标函数
    
    ```python
    from sklearn.model_selection import cross_val_score
    cross_val_score(
        estimator='模型名',
        X='训练集',
        y='测试集',
        cv='交叉验证折数',
        scoring='评估指标',
        n_jobs=-1, #计算单元
        error_score='raise' #拟合失败时的处理(raise抛出错误)
    )
    #scoring参数可选: 
    	#回归: 'r2'决定系数, 'neg_mean_squared_error'负均方误差, 'neg_mean_absolute_error'负平均绝对误差
    	#分类: 'accuracy'准确率, 'recall'召回率, 'precision'精确率, 'roc_auc'ROC_AUC分数, 'f1'F1分数(二分类), 'f1_macro'宏F1分数(多分类)
    	#聚类: 'adjusted_rand_score'调整兰德指数
    #n_jobs参数可选: 默认None表示CPU单核计算, -1表示全部核计算, 或指定具体核心数(正整数)⚠️注意: 并行计算可能增加内存使用
    ```
    
- 交叉验证返回多个评估指标以及时间信息的函数
    
    ```python
    from sklearn.model_selection import cross_validate
    cross_validate(
        estimator='模型名'
        X='训练集',
        y='测试集',
        cv='交叉验证折数',
        scoring='评估指标' #默认score, 多个指标需要以列表或字典的形式传入(['r2', 'precision'])
        n_jobs=-1, #计算单元
        return_train_score=True, #是否返回训练集分数
        return_estimator=False, #是否返回每折训练的模型对象(True时保存每折训练好的模型对象)⚠️可能占用大量内存
        error_score='raise' #拟合失败时的处理(raise抛出错误)
    )
    #该函数返回一个字典
    	#test_score: 多个评估指标为key, test_score为value
    	#train_score: 和test_score形式一致(前提是需要开启return_train_score=True)
    	#fit_time: 每折fit模型的时间(秒)
    	#score_time: 每折模型预测+评估的时间(秒)
    	#estimator: 每折训练好的模型对象(前提是需要开启return_estimator=True)
    ```
    

## 虚拟分类器

- 虚拟分类器可用作构建模型基线benchmark, 检验特征工程是否有效, 检测数据泄漏情况
- 导入虚拟分类器语句:
    
    ```python
    from sklearn.dummy import DummyClassifier as DC
    ```
    
- 虚拟分类器参数详解
    
    ```python
    DC = DC(
        strategy='stratified',
        random_state=42, #随机数种子只对stratified和uniform有效
        constant=None #指定预测值(要和预测标签类型一致, 同时训练集也要存在)
    )
    #strategy参数: 
    	# stratified: 按训练集类别比例随机预测(用于建立性能基线benchmark)
    	# most_frequent: 总是预测出现频率最高的类别(用于评估预测标签不平衡程度)
    	# prior: 和stratified一样, 但是输出的是概率(用于在模型上线前检测生产环境数据分布偏移)
    	# uniform: 所有类别等概率随机预测(用于验证衍生的特征是否有效)
    	# constant: 总是预测用户指定的常量值(需要配合constant参数)
    ```
    

## 偏差-方差-泛化误差

- 当集成模型弱评估器数量大于等于样本数量时(如训练集500个样本, 弱评估器数量600个达到最大分数), 需要进一步计算方差、偏差和泛化误差, 使模型早停训练, 避免无效增加
- 泛化误差=偏差的平方+方差(其中偏差是MSE计算得到(交叉验证取均值), 方差是预测值的方差)+噪声
- 取泛化误差最小的的弱评估器数量
- 导入偏差方差分解语句
    
    ```python
    from mlxtend.evaluate import bias_variance_decomp
    ```
    
- 偏差方差分解参数详解
    
    ```python
    avg_total_loss, avg_bias, avg_var = bias_variance_decomp(
        estimator='实例化配置参数未训练的模型',
        X_train='训练集样本',
        y_train='训练集标签',
        X_test='测试集样本',
        y_test='测试集标签',
        loss='损失函数', #mse回归, 0-1_loss分类
        num_rounds=200, #重采样和模型训练的轮次
        random_seed=42,
        fit_params=None #额外参数
    )
    #返回结果为平均总误差(偏差的平方+方差+噪声)、平均偏差、平均方差
    ```
    
