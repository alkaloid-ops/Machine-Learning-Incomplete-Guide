# 集成算法
## 装袋法Bagging

- 装袋法原理
    - 模型中的各个弱评估器相互独立, 同时并行构建
    - 样本有放回抽样, 特征无放回抽样
    - 最终输出: 回归预测取均值, 分类标签取众数(即少数服从多数)
    - 袋装法目标降低方差来提升模型泛化能力

### 随机森林

### 随机森林回归器

- 使用树类集成算法模型前建议先使用单颗决策树的查看R^2是否超过0.5
- 导入随机森林回归器模型语句:
    
    ```python
    from sklearn.ensemble import RandomForestRegressor as RFR
    ```
    
- 随机森林回归器参数详解
    
    ```python
    RFR = RFR(
        n_estimators=100, #树数量参数(虽然树数量越多模型效果越好, 但对算力要求也越高)
        bootstrap=True, #是否使用有放回抽样
        oob_score=True, #是否使用袋外样本估计泛化精度
        n_jobs=-1, #CPU核心使用数(默认None为单核心, -1为全部核心, 可指定具体核心数值)
        verbose=1, #控制训练过程输出(默认0无输出, 1输出进度条)
        warm_start=False #使用上次调用的参数
    
        #以下参数含义与决策树一致
        criterion='gini','entropy','log_loss','possion'
        max_depth=10,
        max_features='auto',
        max_leaf_nodes=None,
        max_samples=0.8,
        min_samples_leaf=10,
        min_samples_split=10,
        min_weight_fraction_leaf=0,
        min_impurity_decrease=0,
        ccp_alpha=0,
        random_state=42
    )
    ```
    
- 袋外样本估计泛化精度: 当数据量足够大时, 对于有放回抽样, 会有一部分数据最终没有被抽到, 这个概率最终收敛为1-(1/e) 约63%, 即有37%的数据被浪费掉, 那么启用这个袋外样本估计泛化精度, 模型会将些数据作为验证集, 来提升模型的泛化
- 查看袋外样本估计泛化精度结果
    
    ```python
    RFR.oob_score_
    ```
    
- 查看随机森林模型属性
    
    ```python
    RFR.estimators_
    ```
    
- 查看随机森林中每棵决策树的各种属性(确定调参或网格搜索的参数取值范围)
    
    ```python
    [a for a in RFR.estimators_.tree.max_depth] #每棵树的最大深度
    [a for a in RFR.estimators_.tree.node_count] #每棵树的节点数量
    [a for a in RFR.estimators_.tree.n_node_samples] #每个叶子节点的样本量
    [a for a in RFR.estimators_.tree.weighted_n_node_samples] #每个叶子节点的样本权重
    [a for a in RFR.estimators_.tree.impurity] #每个叶子节点的不纯度
    [a for a in RFR.estimators_.tree.threshold] #每个节点分枝后的不纯度下降量(其中负值为叶子节点—无法进一步分枝)
    ```
    
- 对单个节点分枝后的不同不纯度下降量的节点数量
    
    ```python
    pd.Series(RFR.estimators_[0].tree.threshold).value_count()
    np.cumsum(pd.Series(RFR.estimators_[0].tree.threshold).value_count().sort_index()[1:]) #不纯度下降量累加和的节点数量(不计算第一个负值不纯度下降量)(以确定要减少节点数的超参数min_impurity_decrease)
    ```
    
- 模型实例化训练后增加树数量
    
    ```python
    RFR.n_estimators += 1 #增加树数量1
    ```
    

### 随机森林分类器

- 使用树类集成算法模型前最好先使用单颗决策树的查看Accuracy是否超过0.5
- 导入随机森林分类器模型语句:
    
    ```python
    from sklearn.ensemble import RandomForestClassifier as RFC
    ```
    
- 随机森林分类器参数详解
    
    ```python
    RFC = RFC(
        n_estimators=100, #树数量参数(虽然树数量越多模型效果越好, 但对算力要求也越高)
        bootstrap=True, #是否使用有放回抽样
        oob_score=True, #是否使用袋外样本估计泛化精度
        n_jobs=-1, #CPU核心使用数(默认None为单核心, -1为全部核心, 可指定具体核心数值)
        verbose=1, #控制训练过程输出(默认0无输出, 1输出进度条)
        warm_start=False #使用上次调用的参数
    
        #以下参数含义与决策树一致
        criterion='gini','entropy','log_loss',
        max_depth=10,
        max_features='sqrt',
        max_leaf_nodes=None,
        max_samples=0.8,
        min_samples_leaf=10,
        min_samples_split=10,
        class_weight='balanced',
        min_weight_fraction_leaf=0,
        min_impurity_decrease=0,
        ccp_alpha=0,
        random_state=42
    )
    ```
    
- 袋外样本估计泛化精度: 当数据量足够大时, 对于有放回抽样, 会有一部分数据最终没有被抽到, 这个概率最终收敛为1-(1/e) 约63%, 即有37%的数据被浪费掉, 那么启用这个袋外样本估计泛化精度, 模型会将些数据作为验证集, 来提升模型的泛化
- 查看袋外样本估计泛化精度结果
    
    ```python
    RFC.oob_score_
    ```
    
- 查看随机森林模型属性
    
    ```python
    RFC.estimators_
    ```
    

### 自定义基评估器的装袋集成模型

- 导入装袋集成模型语句
    
    ```python
    from sklearn.ensemble import BaggingRegressor #回归
    from sklearn.ensemble import BaggingClassifier #分类
    ```
    
- 装袋集成模型参数详解
    
    ```python
    BaggingRegressor/BaggingClassifier(
        estimator='基评估器', #优先选择高方差模型(在不精调参数下很容易过拟合的模型)
        n_estimators='基评估器数量',
        max_samples=1, #每个基评估器的训练样本量
        max_features=1, #个基评估器使用的特征量
        bootstrap=True, #样本采样是否放回
        bootstrap_features=False, #特征采样是否放回
        oob_score=True, #使用袋外样本评估
        warm_start=False, #是否使用上一次训练的参数
        n_jobs=-1, #计算单元
        random_state=42,
        verbose=0 #过程输出
    )
    ```
    