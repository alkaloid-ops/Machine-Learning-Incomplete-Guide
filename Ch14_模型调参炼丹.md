
# 模型调参

## 网格搜索

- 导入网格搜索语句
    
    ```python
    from sklearn.model_selection import GridSearchCV
    ```
    
- 定义参数字典
    
    ```python
    #以决策树参数为例
    param = {
        'criterion':('gini','entropy'),
        'splitter':('best','random'),
        'max_depth':[*range(10)] #等价于推导式[a for a in range(10)]
    }
    ```
    
- 网格搜索参数详解
    
    ```python
    GS = GridSearchCV(
        estimator='模型名',
        param_grid='参数字典或列表',
        scoring='accuracy','f1','roc_auc', #默认为score
        cv=5, #交叉验证折数, 默认5折
        n_jobs=-1, #CPU计算核心数, 默认1为单核, -1为全部核心, 可指定具体核心数
        refit=True, #是否用最优参数重训练, 默认True, 当有多个评估指标需要指定哪个评估指标用于最佳参数组合重训练
        verbose=0, #输出日志详细度, 默认0不输出, 1输出
        pre_dispatch='2*n_jobs', #控制并行任务分发, 避免内存溢出
        error_score='raise', #当某参数组合拟合出错时的处理, 可设为np.nan忽略
        return_train_score=False, #是否返回训练集得分
    )
    ```
    
- 网格搜索拟合训练数据语句
    
    ```python
    GS.fit(Xtrain, ytrain)
    ```
    
- 获取最优参数组合
    
    ```python
    GS.best_params_
    ```
    
- 获取最优参数在交叉验证中的平均得分
    
    ```python
    GS.best_score_
    ```
    
- 获取最优参数训练好的模型
    
    ```python
    GS.best_estimator_
    ```
    
- 获取参数详解组合的详细结果
    
    ```python
    GS.cv_results_
    ```
    

## 随机网格搜索

- 在原有的网格搜索参数空间中, 随机抽取部分参数组合进行模型训练并输出结果, 抽取次数为迭代次数
- 导入随机网格搜索语句
    
    ```python
    from sklearn.model_selection import RandomizedSearchCV
    ```
    
- 随机网格搜索参数详解
    
    ```python
    RandomizedSearchCV(
        estimator='模型名',
        param_distributions='参数字典',
        n_iter='参数组合数量',
        scoring='评估指标', #回归r2, 分类accuracy
        n_jobs=-1, #计算单元
        refit=True, #对最佳参数组合重训练
        cv=5, #交叉验证
        verbose=0, #过程详情
        pre_dispatch="2*n_jobs", #控制并行任务分发数量(避免内存溢出)
        random_state=42,
        error_score='raise', #模型拟合出错时的替代得分值(默认='raise'抛出异常)
        return_train_score=True #返回训练集分数
    )
    ```
    
- 随机网格搜索参数字典使用连续变量值
    
    ```python
    from scipy.stats import uniform
    params = {"C": uniform(0,50)} #生成一段0-50均匀分布的数值(随机取值)
    ```
    

## 迭代收敛式网格搜索

- 从全部的训练数据中随机无放回抽样出训练集子集, 在子集上网格搜索全部的参数组合, 保留一半得分优异的参数组合, 另一半淘汰, 随后再从训练数据中随机无放回抽样出比之前训练集子集大一倍的新训练集子集, 使用留下的参数组合进行验证, 并淘汰得分较差的一半参数组合, 再抽样再验证再淘汰一半参数组合, 多次迭代直至收敛(多次迭代过程中训练数据集逐渐扩大, 参数组合逐渐减少)
- 当参数组合只剩下1组时或数据集抽样增加到总量时, 迭代收敛式网格搜索停止
- 迭代多次抽样后要尽可能用到全部数据, 且剩余的参数组合不能太多(最好将数据抽样迭代次数和参数组合淘汰剩余1-4个的迭代次数对齐)
- 导入迭代收敛式网格搜索语句
    
    ```python
    from sklearn.experimental import enable_halving_search_cv
    from sklearn.model_selection import HalvingGridSearchCV
    ```
    
- 迭代收敛式网格搜索参数详解
    
    ```python
    HalvingGridSearchCV(
        estimator='模型名',
        param_grid='参数字典',
        factor=2, #参数组合减少比例(n/factor)(n为参数组合量, factor>1)
        resource='n_samples', #增加样本量
        max_resources='auto', #最大资源上限
        min_resources='exhaust', #初始资源量(自动计算最小值)
        aggressive_elimination=False, #激进淘汰模式(True优先使用完所有组合数(重复使用第一次抽样的样本量, 以对齐参数组合的迭代次数),False平衡组合数与资源增长)
        cv=5, #交叉验证
        scoring='评估指标',
        refit=True, #用最佳参数组合重新训练
        n_jobs=-1, #计算单元
        return_train_score=True, #返回训练集得分
        random_state=42,
        error_score='raise', #模型拟合出错时的替代得分值(默认='raise'抛出异常)
        verbose=0 #过程输出
    )
    ```
    

## 贝叶斯优化(基于Optuna)

- 先随机取几个参数组合进行训练模型得到模型分数, 以参数组合为变量模型分数为结果构建函数(代理模型)来预测未使用过的参数取值(采集函数)来寻找最佳参数组合(在未知参数取值探索和已知参数取值利用作平衡)
- 定义目标函数
    
    ```python
    def optuna_objective(trial): #有且仅有一个函数参数trial
        
        #定义超参数取值区间(随机森林为例)
        n_estimators = trial.suggest_int("n_estimators",low=80,high=100,step=1) #整数型,(参数名称,下界,上界,步长)
        max_depth = trial.suggest_int("max_depth",low=10,high=25,step=1)
        max_features = trial.suggest_int("max_features",low=10,high=20,step=1)
        #max_features = trial.suggest_categorical("max_features",["log2","sqrt"]) #字符型
        min_impurity_decrease = trial.suggest_int("min_impurity_decrease",low=0,high=5,step=1)
        #min_impurity_decrease = trial.suggest_float("min_impurity_decrease",low=0.0001,high=0.1,log=False) #浮点型
        
        #定义评估器
        #不需要优化的参数则直接填写具体值
        model = RFR(n_estimators = n_estimators,
                    max_depth = max_depth,
                    max_features = max_features,
                    min_impurity_decrease = min_impurity_decrease,
                    random_state=42,
                    verbose=False,
                    n_jobs=1
                    )
        
        #交叉验证
        #optuna同时支持最大化和最小化,因此如果输出-RMSE,选择最大化, 如果选择输出RMSE,则选择最小化
        cv = KFold(n_splits=5,shuffle=True,random_state=42)
        score = cross_validate(model,
    												   Xtrain,
    												   ytrain,
                               scoring="neg_root_mean_squared_error",
                               cv=cv,
                               verbose=False,
                               n_jobs=1,
                               error_score='raise'
                               )
        
        #最终输出交叉验证的RMSE均值
        return np.mean(score["test_score"])
    ```
    
- 定义优化器函数
    
    ```python
    def optimizer_optuna(n_trials, algo): #函数参数n_trials为允许的最大迭代次数, algo为指定算法TPE还是GP
        
        #定义使用TPE或者GP(两种算法)
        if algo == "TPE": #基于核密度估计的算法
            algo = optuna.samplers.TPESampler(n_startup_trials = 10, n_ei_candidates = 24) #两个参数为初始观测值和期望改进(算法的"探索广度控制器")取值24是大多数研究中被证明是计算效率与结果质量的最佳平衡点
        elif algo == "GP": #基于高斯过程回归的算法
            from optuna.integration import SkoptSampler
            import skopt
            algo = SkoptSampler(skopt_kwargs={'base_estimator':'GP', #选择高斯过程
                                              'n_initial_points':10, #初始观测点10个
                                              'acq_func':'EI'} #选择的采集函数为EI,期望增量
                               )
        #GP算法精度高但计算时间较长, TPE更快但精度不如GP(推荐TPE)
        #实际优化过程,首先实例化优化器
        opt = optuna.create_study(sampler = algo, #要使用的具体算法
                                  direction="maximize" #优化的方向,可以填写minimize或maximize, 交叉验证输出负RMSE取最大值
                                 )
    
        opt.optimize(optuna_objective, #目标函数
                     n_trials=n_trials, #最大迭代次数(包括初始观测值的)
                     show_progress_bar=True #展示进度条
                    )
        
        #打印输出最佳参数组合与最佳损失值
        print(f'best_params:{opt.best_trial.params},best_score:{opt.best_trial.values}')
        
        return opt.best_trial.params, opt.best_trial.values
    ```
    
- 使用优化器
    
    ```python
    best_params, best_score = optimizer_optuna(100, 'TPE') #例如使用TPE法迭代100次
    ```
    